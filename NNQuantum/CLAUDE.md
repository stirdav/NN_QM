# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Scope

This file describes only this folder, `NNQuantum/`, a sibling of `old_version/` and `QuantumDynamics/` at the repo root. It is meant to become a new framework that replaces `old_version`'s dynamics engine with `QuantumDynamics`. Step 1 of the plan below is in progress: `FockLadder_problem.jl` (renamed from `HBAR-qubit_problem.jl`), `Definition.jl`, `Project.toml`, `FockLadder_execution.jl` (renamed from `execution.jl`). See the "Step 1 status" note at the end of the Plan section below for exactly what's live vs. still commented-out pseudocode.

**`test.jl` is `NNQuantum`'s designated test-script location** — smoke tests and validation runs against the code in `FockLadder_problem.jl` go there, not in `FockLadder_execution.jl` (which stays scoped to activating the environment, defining the problem, and — since a later addition, see the Plan section — the generic-`N_steps` dataset-generation loop) or scattered into ad hoc scratch files. See the "Step 2" entry in the Plan section for what `test.jl` currently does.

**`NNQuantum_API.md` is a plain, function-by-function user guide to `NNQuantum.jl`** — what each function does, its inputs/outputs, and a short worked example — written for someone using or extending the library, not for tracking how it was built. This file (`CLAUDE.md`) and `DESIGN.md` stay the project's own working history (why things are the way they are, what was tried, what broke); `NNQuantum_API.md` is the one to read (or hand to someone else) to just use `NNQuantum.jl`.

The two parts below (Part 1, Part 2) analyze `old_version/` and `QuantumDynamics/framework/` **strictly separately** — as two independent, unrelated frameworks, each on its own terms. They propose no mapping, translation, or design decision that connects them. That connection is the subject of the Plan section immediately below, agreed on after both parts were understood on their own.

---

## Plan

Coarse-grained, three-step plan for turning the two independent analyses above into `NNQuantum`'s actual content. Step 1 is broken into sub-points (a–g); Step 1's sub-points are elaborated further in `DESIGN.md`. Steps 2 and 3 are intentionally left coarse — they get their own refinement pass once the step before them is done.

1. **Rewrite the HBAR-qubit problem on top of `QuantumDynamics`** — define the quantum system (subsystems, composite, Hamiltonian, dissipators) and the two-stage pulse protocol, scoped down to a single variant. See sub-points (a)–(g) below and their detail in `DESIGN.md`.
   - a) Port only the `:FL_1step_3p` variant (free drive frequency `ωd`, 3 params). `:FL_1step` and `:FL_1step_2drives` are out of scope. `:FL_1step_3p`'s detuning-correction term (`χ=g²/Δ0`) is dead code in `old_version` (present but commented out, never exercised) — dropped, not ported. **Status: done** — written as a scope comment block in `FockLadder_problem.jl`.
   - b) `N_mech` and `g` are left unbound for now; concrete values (e.g. from Chu et al.) are picked later, right before step 2 needs them to actually run. **Status: superseded** — all physical parameters (`ωq`, `ωm`, `γm`, `Teq`, `nthm`, `κ`, `κϕ`, `Δ0`, `g=258`, `N_mech=5`, Chu et al.) are now bound directly in `FockLadder_problem.jl`, live code. `h`/`hbar`/`kb` live in the new `Definition.jl` (ASCII-minus fix to `old_version/definitions.jl`'s `hbar` typo) — deliberately *not* the QM-side struct builders (`ho`/`qubit`/`qub_ho`), since `QuantumDynamics`'s own subsystem types replace that role (c).
   - c) Build the subsystems and composite system (`Qubit`, `HarmonicOscillator`, `CompositeSystem`) for the qubit + mechanical resonator. **Status: done, live.** `FockLadder_problem.jl`; mechanical subsystem named `:osc` (aligned to `FUNCTION_MAPPING.md`, not the `:mech` first drafted). `Qubit`/`HarmonicOscillator` are constructed with the *pre-transformed* frequencies (`Δ0`, `0.0`), not the bare `ωq`/`ωm` — see (d), which is what makes this the rotating-frame system rather than the lab-frame one.
   - d) Build the bare + JC-coupling Hamiltonian. Provisional: structurally mirrors `old_version`'s rotating-frame `H0`, built from `QuantumDynamics`'s cached operators instead of the `qub_ho` struct. The lab-frame-vs-rotating-frame design question (`QuantumDynamics`'s Hamiltonian recipes are lab-frame only) is explicitly deferred, not resolved here. **Status: done, live — frame question resolved, not deferred.** `H0 = jaynes_cummings(cs, :qubit, :osc, g)`, using the pre-transformed subsystems from (c). This works — exactly, not approximately — because `old_version`'s rotating frame is a *common* rotation `U(t)=exp(iωm·t·(n_q+n_mech))`, and the JC coupling conserves `n_q+n_mech`, so it commutes with `U`'s generator and comes through the transform completely unchanged; only the bare energies shift, `ωq·n_q+ωm·n_mech → Δ0·n_q`. Confirmed by explicit interaction-picture derivation and by running the file: `H0` builds correctly, and the constructed subsystems report `ω = -15800.0` (`= Δ0`) and `ω = 0.0` as expected. The only difference from `old_version`'s own `H0` is a provably inert global-phase term (`Δ0·n_q = 0.5·Δ0·id + 0.5·Δ0·σz`, vs. `old_version`'s `0.5·Δ0·σz` alone) — physically identical, differs only in a direct matrix diff. Same reasoning shows (f)'s dissipators need no frame transform either (their jump operators commute with `U`'s generator up to a phase, which drops out of the Lindblad term).
   - e) Build the two drive terms (`Ω(t)`, `Δ(t)`), reusing `old_version`'s `π_pulse_shape` unchanged, wired in via `add_time_dependence`. **Status: folded into (g), done.** `π_pulse_shape` is real, standalone code (pure math). `Ω(t)`/`Δ(t)`/`add_time_dependence` are *not* separate top-level code: `τ_exc`/`ωd`/`τ_SWAP`/`t0` are per-call pulse parameters, not fixed constants, so they only make sense as function arguments — they're built directly inside (g)'s `FLstep_dynamics_3p`, the one place they're used, rather than duplicated as a free-standing demo.
   - f) Build the dissipators: `thermal_bath` for the mechanical mode (replaces `old_version`'s separate decay+gain jump operators with one call), `Dephasing` and `Decay` for the qubit. **Status: done, live.** `J = jump_operators(cs, [thermal_bath(:osc, γm, nthm)..., Dephasing(:qubit, κϕ), Decay(:qubit, κ)])`.
   - g) Port the two-stage protocol runner (spin-flip stage → SWAP stage, final state of stage 1 feeding stage 2), equivalent to `old_version`'s `FLstep_dynamics_3p`. **Status: done, live.** `FLstep_dynamics_3p(t0, initial_state, τ_exc, ωd, τ_SWAP)`, kept as two sequential `evolve` calls (old_version's "as-is" structure) rather than `QuantumDynamics`'s own single-continuous-schedule example style; that alternative is flagged in-file, not resolved — a genuinely open question, unrelated to (d)'s (now-resolved) frame gap, about `evolve`-call structure instead. Verified end-to-end: called with a test initial state, it returns a well-formed `(tspan, states)` trajectory with `tr(ρ_final) = 1.0` exactly.

     **Bug caught by testing, not review**: the first live draft of this function had `t -> Ω(t) => op(cs,:qubit,:σx)` (no parens) as an `add_time_dependence` argument. `->` binds *looser* than `=>` in Julia, so this parses as `t -> (Ω(t) => op(...))` — one lambda returning a `Pair` each call — not `(t -> Ω(t)) => op(...)`, a `Pair` of a function and an operator, which is what `add_time_dependence` actually needs (confirmed by `add_time_dependence`'s own docstring, which parenthesizes its example the same way). Running the function surfaced a `MethodError` immediately; a plain read-through would very plausibly not have caught this. Fixed by parenthesizing both terms.

   **Step 1 status, as of this session**: all of (a)–(g) done and live — together, they are the *setup* of the HBAR+qubit system (physical parameters, subsystems/composite system, Hamiltonian, dissipators, and the two-stage protocol runner built on top of it), now in `FockLadder_problem.jl` (renamed from `HBAR-qubit_problem.jl`; content unchanged by the rename). Supporting files also added: `Definition.jl` (physical constants only), `Project.toml` (`QuantumDynamics`/`QuantumOptics` deps, `QuantumDynamics` sourced locally via `[sources] = {path = "../QuantumDynamics/framework"}`, no `Manifest.toml` committed — mirrors `QuantumDynamics/framework`'s own examples), `FockLadder_execution.jl` (renamed from `execution.jl`; activates the environment, includes `Definition.jl`/`FockLadder_problem.jl`).

   **Environment: instantiated and verified working.** `julia --project=NNQuantum -e "using Pkg; Pkg.instantiate()"` resolved and precompiled the full dependency tree (`QuantumDynamics` from the local path, `QuantumOptics`, `OrdinaryDiffEqVerner`, `JLD2`, transitive deps) against the locally installed Julia 1.12.7 — no errors. `NNQuantum/Manifest.toml` now exists (pins resolved versions; not discussed above as committed-or-not yet). Sanity check confirmed both packages load and a basic constructor works: `julia --project=NNQuantum -e "using QuantumDynamics, QuantumOptics; Qubit(:qubit, 1.0)"` → succeeds. All of this runs locally (Bash/PowerShell tool calls on the same machine the repo lives on), not remotely.

   Not yet done: no `initial_state` is defined at module level anywhere (only used ad hoc in testing so far), and step 2 (validation run — sweeping realistic `τ_exc`/`ωd`/`τ_SWAP`, plotting `⟨n_qubit⟩`/`⟨n_mech⟩`) hasn't started. `FockLadder_execution.jl` (renamed from `execution.jl`) runs the real physics through (d) on every include (builds `cs`/`H0`); (f)/(g) (`J`, `FLstep_dynamics_3p`) are defined but not called by `FockLadder_execution.jl` itself on include — calling the full protocol was verified separately, not wired into its own run. (`FockLadder_execution.jl` later gained `run_Fock_ladder`, which does call the full protocol — see the step 3 status entries above.)
2. **Validate the rewritten dynamics.** No numerical ground truth exists to check against — `old_version`'s HBAR problem was never run with concrete `N_mech`/`g`. Validation is qualitative: plot `⟨n_qubit⟩` and `⟨n_mech⟩` over the full trajectory and check the expected physical shape (population swap between qubit and mechanical mode across the two stages, dissipation pulling both toward their bath equilibria). Not yet refined further.

   **Status: first smoke test written and passing, in `test.jl`.** Fixes a concrete initial state (`spindown(qubit_basis) ⊗ fockstate(osc_basis, 0)`), runs a short test trajectory through the two-stage protocol, and checks the run succeeded (right number of well-formed states, trace preserved) before plotting `⟨n_qubit⟩`/`⟨n_osc⟩` vs. time and saving to `test_plot.png`. `FLstep_dynamics_3p` itself isn't called directly — it only samples each stage's solver output at the two window endpoints, too coarse to plot — so `test.jl` defines its own `run_short_dynamics`, mirroring `FLstep_dynamics_3p`'s structure exactly (same `H0`/`J`/`π_pulse_shape`, same two-stage hand-off) but sampling each stage at 200 points via `range(...; length=npoints)` instead. `CairoMakie` was added to `Project.toml` as a plotting dependency for this (matching the convention already used by `QuantumDynamics/framework`'s own examples, e.g. `fock_state_preparation/population_ladder.jl`). First run: 399 states, `tr(ρ_final) = 1.0` exactly, no `evolve` normalization warning. Pulse parameters used (`τ_exc=τ_SWAP=1e-3`, `ωd=Δ0`) are short, arbitrary "does it run" values, not physically tuned — the real qualitative-shape validation this step calls for (full population swap, bath-equilibrium approach) is still open and needs parameters actually chosen for that (e.g. `τ_SWAP` comparable to the natural JC swap timescale set by `g`), not this test's.
3. **Translate `old_version`'s ML engine to work with the rewritten dynamics, kept as general as possible** (not hardcoded to the HBAR-qubit problem). What replaces `old_version`'s `Dict{Symbol,Function}` extension mechanism is an open question. Not yet refined further — deferred until step 2 passes.

   **Status: not formally started, but a narrow piece landed early, explicitly requested out of order.** `FockLadder_problem.jl` now also carries `FL_1step_3p_NN_outputs`/`FL_1step_3p_NN_inputs` — direct ports of `old_version`'s dataset-generation functions for the `:FL_1step_3p` variant only (same names, kept for direct correspondence), plus the generic helpers they depend on (`qo_infidelity`, `rand_hermitian_orthonormal_basis`, `threeD_parameter_space`, ported from `old_version/ML_QM_library.jl`; `weighted_sample`, a small hand-rolled replacement for `old_version`'s `StatsBase.sample(...,Weights(...))`, avoiding a new dependency). These are marked in-file as a separate "(h)" addition, not part of (a)-(g)'s system setup, and provisionally colocated in the problem file since no generic library file exists yet — where they actually belong (a struct to replace `fl_1step_features`, a library file, dictionary dispatch or something else) is exactly the open question this step is deferred on, unresolved by this addition. Verified working end-to-end with a scratch smoke test (tiny parameter ranges/sample counts, arbitrary target states): `FL_1step_3p_NN_outputs` samples pulse-parameter triples, `FL_1step_3p_NN_inputs` runs each through `FLstep_dynamics_3p` and returns one feature row per sample (`decom_basis` expectation values + 2 infidelities), row length matching `d²+2` exactly for the composite dimension `d`.

   **Status update: dataset persistence added.** `FockLadder_problem.jl` now also has `save_dataset(path, inputs, outputs; ...)`, saving the `inputs`/`outputs` pair above to a `.jld2` file, one row per sample as `vcat(inputs[i], outputs[i])` — matches `old_version`'s `dataset_creation` row layout (input columns then output columns). Follows `QuantumDynamics/framework`'s own `io.jl` convention (`save_result`): separate top-level JLD2 keys plus a `format_version`, not one serialized blob. `JLD2` added as a direct `Project.toml` dependency (it was already a transitive dependency via `QuantumDynamics`, but a direct `using JLD2` needs it declared directly too) via `julia --project=NNQuantum -e "using Pkg; Pkg.add(\"JLD2\")"`, same real-command pattern the environment's own instantiation used. Verified end-to-end: saved a 4-sample smoke dataset, reloaded it, and confirmed the saved matrix's first row matches `vcat(inputs[1], outputs[1])` exactly.

   **Status update: dataset-generation smoke test added to `test.jl`, mirroring `Chu_DFL_execution.ipynb`.** `test.jl`'s section 4 reproduces the `:FL_1step_3p`/`:master_dynamic` cell of `old_version/HBAR-qubit_problem/Chu_DFL_execution.ipynb` (its `g=258` section — no rescaling needed against `FockLadder_problem.jl`'s own `g`), restricted to ladder step 1 and `n_samples=5` (that cell's own 800, scaled down for a smoke test) — dataset generation and saving only, no NN training (that cell's NN half has no NNQuantum counterpart yet). Target states (`spinup(qubit)⊗|0⟩` for the spin-flip stage, `spindown(qubit)⊗|1⟩` for the SWAP stage) mirror the notebook's `creation_step_states(ρ0, 1)`; parameter ranges (`τ_exc∈[1e-5,5e-2]`, `ωd∈[Δ0-100,Δ0+100]`, `τ_SWAP` centered on `t_swap_th=π/(2g√step)`) and `dim_parameters_space=[100,100,100]` are ported unchanged from that same cell. Run end-to-end (`julia --project=NNQuantum NNQuantum/test.jl`): sampled 5 `(τ_exc,ωd,τ_SWAP)` triples, generated 5 feature rows of 146 values each, and saved them via `save_dataset` to `NNQuantum/dataset_step1.jld2` (reload-verified: correct shape `(5,149)`, first row's last 3 entries match the sampled pulse parameters). `dataset_step1.jld2` is a generated artifact, left untracked like `test_plot.png`.

   **Status update: `execution.jl` renamed `FockLadder_execution.jl`, and given the generic-`N_steps` dataset-generation structure `test.jl`'s step-1-only smoke test doesn't cover.** `run_Fock_ladder(N_steps; n_samples=800, dim_parameters_space=[100,100,100], save_dir=@__DIR__)`, structurally mirroring `Chu_DFL_execution.ipynb`'s `execution_dynamic_FL` (same cell as `test.jl`'s smoke test, generalized from `step=1` to a `for step in 1:N_steps` loop): per step, builds that step's target states and parameter ranges (`τ_SWAP` narrows via `t_swap_th=π/(2g√step)` as the ladder climbs), generates+saves a dataset via `FL_1step_3p_NN_outputs`/`FL_1step_3p_NN_inputs`/`save_dataset`. The NN training/prediction half of `execution_dynamic_FL` is left as an in-loop comment, not code, per this task's request — step 3 is still unstarted, so there's no learned "best pulse parameters" yet. Without that prediction, the loop can't advance `t0`/the carried-forward state the way `old_version` does (`t0 + sum(time), final_state` from the NN's own prediction); it uses a documented placeholder instead — each step's *target* state and nominal (midpoint/threshold) pulse timing, i.e. assumes the ladder climbs perfectly. Clearly flagged in-file as provisional, meant to be replaced once NN prediction exists. Not called on `include` (real `n_samples` is expensive) — verified instead with a cheap explicit call, `run_Fock_ladder(2; n_samples=3, save_dir=tempdir())`: both steps' datasets saved correctly (shape `(3,149)` each), and `t0`/`params[:t0]` advanced between steps as expected (`0.0` → `≈0.031`).

   **Status update: `run_Fock_ladder` restructured around the user's actual train/predict/reiterate algorithm; the target-state placeholder above was removed, not fixed.** The user specified the intended step-3 loop explicitly: 0) start from the true ground state; 1) generate a dataset from the current initial condition over a spectrum of drive values; 2) train a NN on it; 3) use the NN to predict the drive parameters reaching the target state; 4) reiterate 1-3 using the state the *predicted* (imperfect) pulse actually reaches as the next step's initial condition, until `N_steps` is done. The previous version's placeholder — advancing with each step's *target* state and nominal pulse timing, i.e. assuming perfect climbing — was flagged by the user as not matching this algorithm at all (the entire point of steps 2-4 is to advance from what a real, imperfect prediction reaches, not from the target itself), so it's gone rather than patched. `run_Fock_ladder` is now structured as five numbered sections matching 0-4 above: step 0 (ground state) before the loop; step 1 (dataset generation, unchanged, live) inside it; steps 2 (`train_NN`) and 3 (`predict_drive_parameters`) as explicit unimplemented stubs (`= nothing`, with the intended call commented alongside); step 4 guarded — if `predicted_params` is `nothing` (always, currently), the loop `@warn`s and `break`s after step 1's dataset rather than fabricating an advance. So today, calling `run_Fock_ladder(N_steps)` for any `N_steps` only ever produces `dataset_step1.jld2` and then stops. Full step 4 (calling `FLstep_dynamics_3p` with predicted parameters and carrying forward `states[end]`) is written and live, just unreachable until steps 2-3 exist. See `DESIGN.md` Part 5 for the detailed before/after.

   **Plan for today — not yet started.** Five sub-steps, aimed at closing the `train_NN`/`predict_drive_parameters` stubs above:
   - (i) Create `NNQuantum.jl` as the project's NN library file — the option left open when (h) provisionally colocated its generic helpers (`qo_infidelity`, `rand_hermitian_orthonormal_basis`, `threeD_parameter_space`) in `FockLadder_problem.jl` for lack of one. Intended split (to be confirmed once (i)/(ii) are underway, not fully fixed yet): `NNQuantum.jl` holds the problem-agnostic NN machinery, mirroring `old_version/ML_QM_library.jl`'s role; `FockLadder_problem.jl`'s `train_NN`/`predict_drive_parameters` stubs become thin `:FL_1step_3p`-specific wrappers calling into it, and `run_Fock_ladder` itself stays in `FockLadder_execution.jl`. **Status: done.** `NNQuantum.jl` now exists, holding the three named functions (`weighted_sample` deliberately excluded — see `DESIGN.md` Part 6 (i)'s status note); `FockLadder_problem.jl` includes it and dropped its now-unneeded `using LinearAlgebra`. Verified: `FockLadder_execution.jl` runs end-to-end (exit 0), and `run_Fock_ladder(1; n_samples=3, dim_parameters_space=[3,3,3], save_dir=tempdir())` exercises the relocated functions correctly.
   - (ii) Port the relevant functions from `old_version/ML_QM_library.jl` (training loop, normalization, loss dispatch, prediction orchestration) into `NNQuantum.jl`, rebased to run on `QuantumDynamics`-produced data — the `save_dataset`/`dataset_rows` JLD2 format from (h) and `FLstep_dynamics_3p` for re-simulation/scoring — instead of `old_version`'s `Dict{Symbol,Function}` dispatch. Needs a new direct `Project.toml` dependency, `Flux` (plus its transitive deps `Zygote`/`Optimisers`) — substantially heavier than `CairoMakie`/`JLD2`, flagged but not yet added. **Status: done.** `Flux` added (`v0.16.11`, real `Pkg.add`). `NNQuantum.jl` gained `load_dataset`/`train_test_split` (JLD2-backed, reading `dim_input`/`dim_output`), the min-max normalization functions, `LOSS_FUNCTIONS`-dispatched loss functions (one bug fixed in porting: `old_version`'s `:mae` pointed at MSE, now correctly `Flux.Losses.mae`), `train_model!`/`test_model` (renamed from `training!`/`testing_model_infidelity` — the old name overpromised, since its infidelity code was already dead in `old_version`), and `predict_output`/`predict_and_score` — the latter takes the dynamics call as a plain function argument (`simulate`) rather than a dict lookup, so it stays problem-agnostic while still driving a problem-specific simulation once a caller (e.g. (iv)'s not-yet-written `predict_drive_parameters`) passes a closure over `FLstep_dynamics_3p`. Verified with a scratch smoke test (not `test.jl` — that's (iii)): generate→save→reload→split→normalize→train→test→predict-and-score against the real `FLstep_dynamics_3p`, well-formed output throughout (infidelity `≈0.986`, expected for an untrained model on 8 samples). `FockLadder_execution.jl`/`test.jl` both re-run clean afterward. **Status update, later this session:** `train_model!`/`test_model` revised from the straight per-sample port to genuine shuffled mini-batch training (`batch_size` keyword, default `32`) — a deliberate behavior change flagged in-file, not a silent refactor, since the per-sample loop doesn't scale to this project's real hyperparameters (~280,000 individual gradient steps per Fock-ladder step at `n_samples=800`/`epochs=350`) and never shuffled row order between epochs the way `old_version` didn't either. `loss` now accepts a plain `Function` or a `Symbol` (`LOSS_FUNCTIONS`-resolved), and `train_and_test_NN` gained matching `batch_size`/`model`/`opt_state` keywords (the latter two let a caller supply its own architecture instead of `build_default_model`'s fixed one). See `DESIGN.md` Part 6 (ii) for full detail, including both status updates.
   - (iii) A smoke test for the ported NN machinery, in `test.jl` — same convention as every prior smoke test (small `n_samples`/dims, checks shape and that it runs, not physical accuracy).
   - (iv) Write the actual NN half into `run_Fock_ladder`'s steps 2-3 (the restructure above), following `Chu_DFL_execution.ipynb`'s NN cell structure — `old_version`'s own execution-file counterpart — rather than inventing a new orchestration shape. **Status: done.** `NNQuantum.jl` gained two more generic pieces (`build_default_model`, `train_and_test_NN`, ported from the notebook's own architecture/hyperparameters) so that `FockLadder_problem.jl`'s new `train_NN`/`predict_drive_parameters` wrappers could stay genuinely thin — black-box per this session's explicit requirement: nothing in `NNQuantum.jl` references a pulse parameter, `decom_basis`, or `FLstep_dynamics_3p` by name, so the same machinery works for any quantum problem or drive parameterization, not just `:FL_1step_3p`. `run_Fock_ladder`'s steps 2-4 are fully wired (no more `= nothing`/`@warn`+`break` guard); step 4 now carries forward `predict_and_score`'s own re-simulated `final_state` directly rather than re-simulating a second time. One bug caught by testing: `train_NN`'s first draft mishandled `outputs` being `Vector{NTuple{3,Float64}}` (tuples, not vectors like `inputs`), producing a malformed 1-row matrix — fixed with `collect.(outputs)`. Verified with `run_Fock_ladder(2; n_samples=6, dim_parameters_space=[3,3,3], epochs=3, save_dir=tempdir())`: both steps complete in full, well-formed predictions and infidelities. `FockLadder_execution.jl`/`test.jl` both still exit 0. `run_Fock_ladder` takes six NN-related keyword arguments in total — `train_fraction`/`hidden`/`η`/`epochs`/`loss` plus `batch_size` (added alongside (ii)'s mini-batch revision above; no `Chu_DFL_execution.ipynb` equivalent, since that cell never batches). See `DESIGN.md` Part 6 (iv) for full detail.
   - (v) Staged validation once (iv) closes step 4's guard: first `run_Fock_ladder` up to Fock step 1 only, confirming the full 0-4 loop actually completes (not just reaches step 1's dataset and `break`s, today's guarded behavior); then, if needed, up to Fock step 2.

   See `DESIGN.md` Part 6 for detail, including two open considerations not yet resolved: the exact `NNQuantum.jl`/`FockLadder_problem.jl` generic-vs-specific boundary, and the `Flux` dependency's cost relative to the project's minimal-deps stance so far.

   **A later session's side quest: two optimizations to ported `NNQuantum.jl` functions.** `rand_hermitian_orthonormal_basis` no longer indexes `qr`'s lazy `Q` factor `d²` times in a loop (materializes it once instead — same output, verified against the orthonormality property). `FL_1step_3p_NN_outputs` gained a `uniform::Bool` keyword that skips the dense-grid-then-weighted-sample path (`O(∏dim_parameters_space)`) in favor of direct continuous sampling (`O(n_samples)`) when the sampling weight is uniform — true for every live caller in this project, so `run_Fock_ladder` now defaults to `uniform=true`. See `DESIGN.md` Part 7 for detail and verification.

   **Another later addition: `run_Fock_ladder` can now reuse a saved dataset or a saved NN, per step, instead of always making both fresh.** New `dataset_mode` (`:generate_and_save`/`:generate_only`/`:load`) and `nn_mode` (`:train_new`/`:continue_training`/`:fixed`) arguments, each settable once for all steps or as one value per step. This needed `NNQuantum.jl` to gain NN save/load (`save_nn`/`load_nn`) and a second `train_NN` method for plain-matrix data (so a loaded dataset trains the same way a freshly-made one does). Testing this found a real bug: `decom_basis` (the random operator set every feature vector is built from) was made fresh, unseeded, every call — so a dataset or NN saved in one call didn't match the basis used when reloading it in another call, giving wrong predictions with no error at all. See `DESIGN.md` Part 8 for the full story and how it was verified.

   **Then made safer again, per user request: basis-making became its own step, and file names carry a basis number.** `run_Fock_ladder` no longer makes a basis by itself at all — a new function, `generate_decom_basis(basis_id)`, makes one on purpose and saves it as `decom_basis_b<basis_id>.jld2`. `run_Fock_ladder` now takes `basis_id` as a required argument and only ever loads that file (stopping with a clear error if it's missing). Every dataset/NN file it saves ends with the same `basis_id`, e.g. `dataset_step1_b1.jld2`/`nn_step1_b1.jld2`, so file names alone show what belongs together, and `dataset_mode=:load`/`nn_mode ∈ (:continue_training,:fixed)` find these files by themselves without needing an explicit path. See `DESIGN.md` Part 9.

   **Two prerequisites for (v)'s staged validation, checked/added before starting it.** Dataset save/reopen was already there and working (`save_dataset`/`load_dataset`, already exercised by `dataset_mode=:load`) — nothing added. Trajectory plotting for chosen observables was genuinely missing — added as `NNQuantum.jl`'s `plot_trajectory(tspan, states, observables; ...)`, problem-agnostic like the rest of that file, plus `FockLadder_problem.jl`'s `FLstep_dynamics_3p_dense` (promoted from `test.jl`'s own test-local helper) to actually supply a plottable — not just 3-point — predicted trajectory. New standing rule from the user: any function added to `NNQuantum.jl` gets `NNQuantum_API.md` updated in the same pass — done here. See `DESIGN.md` Part 11.

   **Status: (v)'s staged validation run and confirmed successful, both N_steps=1 and N_steps=2.** Per the user's explicit scoping choice, this was done via a copy of `run_Fock_ladder`/`generate_decom_basis` inside `test.jl` (saving predictions, plotting a per-step and one cumulative trajectory, returning `(decom_basis, infidelities, predictions, models, final_states)` — matching `Chu_DFL_execution.ipynb`'s own `execution_dynamic_FL` return shape, per explicit request) — `FockLadder_execution.jl`'s own `run_Fock_ladder` was left untouched. At small/fast hyperparameters (`n_samples=150`, `epochs=60`, `hidden=128`), N_steps=1 gave infidelity 0.044; N_steps=2 reused step 1's already-reviewed NN exactly (`nn_mode=:fixed`, bit-identical prediction) and trained step 2 fresh (infidelity 0.081), with the cumulative plot showing `⟨n_osc⟩` stepping `0→~0.9→~1.8` — a clean two-rung ladder climb, chained from the *predicted* state at each step, not the target. Both runs visually confirmed by the user; logged in the new `NNQuantum/test_log.md`. See `DESIGN.md` Part 12. Left open: whether to promote the now-more-capable `test.jl` copy back into `FockLadder_execution.jl`'s own `run_Fock_ladder`.

---

## Part 1 — `old_version/` (`ML_QM`)

A Julia framework (`QuantumOptics.jl` + `Flux.jl`) that inverts a quantum control problem: given a target quantum state, find the pulse parameters that drive a system to it. Two decoupled halves connected only by the dataset:
- **Dynamics engine** — pure physics. Builds a time-dependent Hamiltonian (and Lindblad operators, for dissipative runs) from pulse parameters and integrates the trajectory (`dynamic_evolution` in `ML_QM_library.jl`, dict-dispatched over `:schroedinger`/`:schroedinger_dynamic`/`:master`/`:master_dynamic`, fixed-step `dt`, `adaptive=false`, `reltol=abstol=1e-9`).
- **Learning engine** — treats the dynamics engine as a black-box data generator: samples pulse parameters, simulates, builds a supervised dataset (state features → pulse params), trains a `Flux.Chain`, and uses it to predict pulses for a target state, then re-simulates and scores by infidelity.

**Extension mechanism**: no abstract problem interface — dispatch is two global `Dict{Symbol,Function}` (`dataset_problems_dictionary`, `plot_problem_dictionary`) mapping a `problem::Symbol` to `[inputs_fn, outputs_fn]` / a plotting-dynamics function. A new problem supplies four functions (`problem_prototype.jl` is the commented-out template): `..._NN_outputs` (samples pulse parameters), `..._NN_inputs` (runs dynamics, builds NN feature vectors), `..._dynamics` (runs the evolution), `create_..._dynamics` (builds the Hamiltonian closure + Lindblad operators).

**QM-side structs** (`definitions.jl`): `ho{ho_basis}` (harmonic oscillator: `basis,a,ad,n,Id`), `qubit` (spin-1/2: `basis,σm,σp,σx,σy,σz,Id`), `qub_ho{ho_basis}` (tensor product with pre-built composite operators: `zI,xI,mI,pI,II,pa,mad,Ia,Iad,n_mech,n_qubit`), built via `Harmonic_oscillator`/`Qubit`/`Qubit_HO`. No caching abstraction beyond this — every problem hand-picks which pre-tensored operator it needs.

**Time-dependent Hamiltonians**: built as a `LazySum` whose `factors` are mutated in place inside a `Hamiltonian(t,ψ)` closure (needed for performance under `timeevolution.schroedinger_dynamic`/`master_dynamic`, which call it every solver step) — not `QuantumOptics.jl`'s newer `TimeDependentSum`.

**The one worked example**: `HBAR-qubit_problem/` — a mechanical resonator (HBAR) Jaynes-Cummings-coupled to a qubit (Chu et al. parameters), Hamiltonian written in the frame rotating at the mechanical frequency `ωm` (so the qubit's bare energy collapses to the detuning `Δ0 = ωq-ωm` and the mechanical bare term vanishes entirely). Two-stage "FL_1step" pulse protocol (spin-flip, then SWAP), in three registered variants differing only in how the pulse is parameterized: `:FL_1step` (2 params, fixed `Ω_R=π/τ_exc`, drive carrier `cos(Δ0_tilde·t)`), `:FL_1step_3p` (3 params, free drive frequency `ωd` replacing the fixed carrier — its optional detuning-correction term `χ=g²/Δ0` is present in the code but commented out, so it's currently always inactive), `:FL_1step_2drives` (both stages independently BSpline-shaped via `generate_Bspline_basis`/`Bspline_composition`/`drive_from_normalized_spline`, needs a `basis_spline` set up first).

**Never run with concrete numbers**: `N_mech` (mechanical Fock cutoff) is a `...` placeholder in the unfinished template `ML_QM_execution.jl`; the JC coupling `g` is commented out (`#g = 258`) in `HBAR-qubit_problem.jl`. There is no known-good numerical output anywhere in the repo to validate against.

**Self-documented unfinished/fragile bits**: dissipative branches marked `#to be fixed`; `dynamics_n_steps_FL` (multi-step chaining) marked `# fix it`; `ML_QM_execution.jl` is a non-runnable template.

**No tests, no linter.** Validated by running `HBAR-qubit_problem/Chu_DFL_execution.ipynb` interactively and inspecting plots/infidelity values. Single Julia environment, rooted at `HBAR-qubit_problem/` (~24 deps: `Flux`, `DifferentialEquations`, `Zygote`, `PlotlyJS`, `CSV`, `DataFrames`, `BSplineKit`, `Distributions`, `StatsBase`, `ProgressMeter`, ...) — loaded together on every run regardless of whether the learning engine is actually used.

---

## Part 2 — `QuantumDynamics/framework`

A general, **tested** (137 passing tests) Julia framework for cavity/circuit QED simulation, built as a thin organizational layer over `QuantumOptics.jl`. Not an ML framework — no dataset/training concept anywhere in it. Reimplements no Hilbert-space or solver machinery; adds reusable subsystems, a composite-system abstraction, Hamiltonian recipes, dissipation channels, and one hardened `evolve` entry point.

**Subsystems** (`subsystems.jl`): `AbstractSubsystem` supertype. `Qubit(name,ω)`, `HarmonicOscillator(name,ω;nmax=10)`, `Transmon(name,ω,α;nmax=4)` — each **precomputes and caches** its operator set (`ops::NamedTuple`) at construction, including a uniformly-defined excitation number `n` (0 = ground, integer-valued for every subsystem type) and a bare local Hamiltonian `h0` (`ω*n`, plus an anharmonic correction for `Transmon`). Load-bearing sign-convention gotcha: `Qubit`'s `n = (id+σz)/2`, not `-σz`, because `QuantumOptics.jl`'s `sigmam` treats spin-up as the excited state — getting this backwards silently breaks excitation-number conservation in coupling terms while still looking Hermitian and well-formed. Uniform accessor: `op(s, key)`.

**Composite systems** (`composite.jl`): `CompositeSystem(subs...)` tensors several subsystems into one joint Hilbert space and **eagerly embeds every subsystem operator** into it once, at construction (not lazily) — the expensive step happens once, not on every Hamiltonian build or solver step. Subsystem names must be unique (checked, `ArgumentError` otherwise). `op(cs,name,key)` looks up a cached embedded operator; `embed(cs,name,local_op)` embeds an arbitrary one-off operator. Extending `QuantumOptics.jl`'s own `embed`/`ptrace` requires an explicit `import` (not just `using`), documented as a easy-to-get-wrong gotcha.

**Hamiltonian recipes** (`hamiltonians.jl`), all built purely from cached embedded operators (lab frame, i.e. bare energies are `ωq·nq + ωo·no`, always): `bare_hamiltonian` (Σ h0); `jaynes_cummings`/`rabi`/`tavis_cummings` (RWA / non-RWA / multi-qubit coupling, via a `_lower`/`_raise` dispatch that treats `Qubit` and `Transmon` uniformly as "qubit-like"); `dispersive_hamiltonian` (qubit-only, `χ=g²/Δ`, rejects `Δ=0`); `quadratic_coupling` (qubit-only, `g(a+ad)²σz`, optomechanical-type, does not conserve excitation number). `add_time_dependence(H, coeff=>op, ...)` turns any static `H` into a `TimeDependentSum` — design rule: coefficients carry the time dependence, operators are always one of the cached, fixed ones.

**Dissipation** (`dissipation.jl`): `Decay(name,rate;nth=0)`, `Gain(name,rate;nth)` (no default `nth` — only meaningful paired with `Decay`), `Dephasing(name,rate)` — each a typed `Dissipator` producing one rate-scaled jump operator via `jump_operator`/`jump_operators`, dispatched per subsystem type. `thermal_bath(name,rate,nth)` builds a matched `[Decay,Gain]` pair so the two can't desync. `Dephasing`'s jump operator is `sqrt(rate/2)*op` specifically so `rate` is directly `1/Tφ` (documented, tested convention).

**Solver wrapper** (`evolution.jl`): a single `evolve(tspan, state, H[, J])` dispatches on presence of `J` (open/closed) and the type of `H` (time-dependent or not) to the matching `QuantumOptics.timeevolution.*` function. Overrides `QuantumOptics.jl`'s own `DP5()`/`1e-6`/`1e-8` defaults with `Vern9()`/`1e-8`/`1e-10`, because `DP5` can silently accumulate O(1) phase error on a Hamiltonian with a sustained fast oscillation (a documented, tested failure mode). Warns (doesn't error) if the final state's norm/trace drifts from 1 by more than `1e-4`. Also documents a *second*, distinct adaptive-solver failure mode — a short, narrow pulse feature the solver's step size can jump straight over without ever sampling it — which tighter tolerance/higher order does **not** fix; the documented fix is `tstops`/`dtmax`/fixed `dt`.

**Measurement** (`measurement.jl`): `ptrace`, `condition_on` (project+trace+renormalize, returns `(ρ_cond, prob)` so a near-zero-probability outcome is visible rather than silently `NaN`), `check_fock_cutoff` (`Ket`-only pre-flight truncation guard).

**Persistence** (`io.jl`): `SimulationResult`/`save_result`/`load_result` (JLD2-backed), stores the minimal reconstructible spec (`subsystems`, `dissipators`) rather than the derived, larger `cs`/`J`; time-dependent `H` only round-trips reliably if its coefficient is a named function, not an anonymous closure (documented, tested limitation).

**Dependencies**: `QuantumOptics.jl`, `OrdinaryDiffEqVerner` (for `Vern9`), `JLD2` — three, deliberately minimal. **137 passing tests** (`test/runtests.jl`), including sanity-control tests that verify the *absence* of a property (e.g. `rabi`/`quadratic_coupling` genuinely don't conserve excitation number) to prove the corresponding positive test isn't vacuous. Three worked examples under `examples/`, each with its own environment.
