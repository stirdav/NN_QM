# DESIGN.md

Deeper analysis backing `CLAUDE.md`. Same rule applies to Part 1 and Part 2 below: independent analyses of `old_version/` and `QuantumDynamics/framework`, kept strictly separate, proposing no mapping or translation between them. Part 3 is different — it elaborates the Step 1 sub-points (a)–(g) from `CLAUDE.md`'s Plan section, which is where the two are deliberately brought together.

---

## Part 1 — `old_version/` (`ML_QM`) in detail

### Struct inventory (`definitions.jl`)

- `ho{ho_basis}`: `basis, a, ad, n, Id`. Built by `Harmonic_oscillator(N_particle::Int64, type_basis::Symbol)` — `type_basis` is looked up in a one-entry `Dict(:FockBasis => FockBasis)`, i.e. a dictionary dispatch used even where there's only one option.
- `qubit`: `basis, σm, σp, σx, σy, σz, Id`, all typed to `SpinBasis{1//2,Int64}` specifically (not generic spin). Built by `Qubit(spin)`.
- `qub_ho{ho_basis}`: the tensor product, with every commonly-needed composite operator pre-built as a field: `zI, xI, mI, pI, II` (qubit operator ⊗ oscillator identity), `pa, mad` (Jaynes-Cummings-style coupling terms: `σp⊗a`, `σm⊗ad`), `Ia, Iad` (qubit identity ⊗ oscillator ladder), `n_mech, n_qubit` (number operators, `n_qubit` built as `0.5*(II + zI)` — i.e. the same `(id+σz)/2` convention). Built by `Qubit_HO(N_mech, type_basis_mech::Symbol, type_basis_qubit)`, returning `(qub, mech_res, qub_ho_struct)` as a 3-tuple.
- Pipeline-config structs, all loosely typed (`Any` fields, validated by convention not the type system): `dataset_features` (`problem::Symbol` is the dictionary-dispatch key; `len_dataset=[n_samples,n_training]`; `dim_dataset=[n_input,n_output]`; parameter-space bounds/dims; sampling probability function `pr`; `dynamics::Symbol`; `t0`; `initial_state`; `problem_features`; `names_dataset`; `modality_dataset` (`:generating`/`:generating_and_saving`/import); `norm_dataset`), `step_states` (initial/target kets+density matrices for one protocol step), `fl_1step_features` (target states, decomposition basis, phonon number, `correction::Symbol`), `fl_1step_nn_features_` (`model::Flux.Chain`, `N_epochs`, `η`, `optimizer`, `features_prediction::Vector{Any}` — a positional bundle consumed by the prediction function, `loss_func::Symbol`, `type_prediction::Symbol`).

### Physical constants (`definitions.jl`)

```julia
const h    = 6.62607015e-34
const hbar = 1.054571817e−34   # note: the character before "34" reads as a Unicode minus (U+2212),
                                # not ASCII "-" — worth double-checking if this file is ever
                                # actually `include`d and evaluated, since Julia numeric literals
                                # need an ASCII sign in the exponent.
const kb   = 1.380649e-23
```

### HBAR-qubit problem (`HBAR-qubit_problem/HBAR-qubit_problem.jl`)

Parameters (Chu et al.): `ωm = 5.9614e6`, `ωq = 5.9456e6` (labeled `[KHz]`), `γm = 0.025` (mechanical dissipation rate), `Teq = kb/(2π·hbar)·1e-3·10e-3`, `nthm = 1/(exp(ωm/Teq)-1)` (mechanical bath thermal occupation), `κ = 19` (qubit decay), `κϕ = 0.25` (qubit dephasing), `Δ0 = ωq - ωm`. `basis = tensor(SpinBasis(1//2), FockBasis(N_mech))`; `qub, mech_res, qubit_mech = Qubit_HO(N_mech, :FockBasis, 1//2)` — both executed at file-include time, so `N_mech` must already be a bound global before this file is included.

Rotating-frame Hamiltonian (frame rotating at `ωm`), shared structure across all three variants:
```julia
H_JC = g * (qubit_mech.pI*qubit_mech.Ia + qubit_mech.mI*qubit_mech.Iad)
H0   = 0.5 * Δ0_tilde * qubit_mech.zI + H_JC
```
where `Δ0_tilde = Δ0` normally, or `Δ0 + 2χ(n_phonon+0.5)` with `χ=g²/Δ0` if `typeofcorrection == :correction_on` (only wired up for the base `:FL_1step` variant — `:FL_1step_3p`'s correction block is present but commented out).

Time-dependent part built as a **mutated `LazySum`**, not `QuantumOptics.jl`'s `TimeDependentSum`:
```julia
Ω(t) = Ω_R * π_pulse_shape(t, t0, τ_exc) * cos(carrier_arg*t)   # carrier_arg = Δ0_tilde for :FL_1step, ωd for :FL_1step_3p
Δ(t) = -Δ0_tilde * π_pulse_shape(t, t0+τ_exc, τ_SWAP)
Ht = LazySum([Ω(t0), Δ(t0)/2], [qubit_mech.xI, qubit_mech.zI])
function Hamiltonian(t, ψ)
    Ht.factors[1] = Ω(t)
    Ht.factors[2] = Δ(t)/2
    return H0 + Ht
end
```
`:FL_1step_2drives` extends this to three mutated factors (`Ω1, Ω2, Δ`), with `Ω1`/`Ω2` built from BSpline-composed coefficients (`Bspline_composition(coeffs, basis)` returns `x -> spline(x)`; `drive_from_normalized_spline(spline, T)` remaps `t∈[0,T]` to the spline's `[0,1]` domain) instead of a fixed `cos` carrier.

Dissipators (four fixed jump operators, always the same shape regardless of variant):
```julia
[sqrt(γm*(nthm+1))*qubit_mech.Ia, sqrt(γm*nthm)*qubit_mech.Iad, sqrt(κϕ/2)*qubit_mech.zI, sqrt(κ)*qubit_mech.mI]
```
plus their `dagger.(...)` — both the operators and their daggers are returned from `create_..._dynamics`, i.e. the daggers are computed once up front rather than by the solver.

Two-stage runner (`FLstep_dynamics`/`FLstep_dynamics_3p`/`FLstep_2drives_dynamics` — three near-identical copies, one per variant): builds one Hamiltonian+dissipator set via `create_..._dynamics`, then calls `dynamic_evolution` twice — once over `[t0, t0+τ_exc]` (spin-flip stage), once over `[t0+τ_exc, t0+τ_exc+τ_SWAP]` (SWAP stage), feeding the first stage's final state into the second. Branches on `typeofdynamics ∈ {:master,:master_dynamic}` (dissipative, density-operator path, passes `(H,J,J†)` as the "state derivative input") vs. else (unitary, ket path, passes `H` alone). A second, separate `modeofdynamics::Symbol` argument controls what's returned: `:dynamics` → `(ρ_at_flip, ρ_at_end)`; `:final_state` → `(t_end, ρ_at_end)`; `:all_dynamics` → full concatenated `(tspan, ρ_out)`; anything else (the default, used for plotting) → `(tspan, ρ_out, τ_exc, τ_SWAP)`. Four different return shapes from one function, selected by a runtime symbol.

### Dataset/NN plumbing (`ML_QM_library.jl`)

No `using` statements in this file at all — it relies entirely on whatever the including script/notebook has already loaded (`QuantumOptics`, `PlotlyJS`, `StatsBase`, `Distributions`, `CSV`, `DataFrames`, `Flux`, `ProgressMeter`, `BSplineKit`, ...). Nothing at its own top level requires those beyond `QuantumOptics`-typed function signatures; everything else is only touched inside function bodies, so it works as long as the *caller* did the right `using`s first.

- `dynamic_evolution(time, ψ0, dynamics_input, type_dynamics::Symbol)`: `tspan = time[1]:time[2]:time[end]`; dict-dispatches `type_dynamics` to one of `timeevolution.{schroedinger,schroedinger_dynamic,master,master_dynamic}`; always calls with `adaptive=false, dt=time[2], reltol=1e-9, abstol=1e-9` — i.e. fixed-step integration regardless of what tolerance is requested (the tolerance kwargs are dead in the `adaptive=false` case, since `OrdinaryDiffEq`'s fixed-step solvers ignore them).
- `π_pulse_shape(t,t0,duration,eps=1e-12)`: `sin(π·δt/duration)² / (sin(...)² + eps²)` inside the window, `0.0` outside — the `eps`-regularized ratio forces the value to exactly `1.0` at the window's interior peak and decays the usual sin² shape elsewhere, while the `if 0≤δt<duration` branch forces an exact, non-decaying `0.0` outside (rather than relying on the sin² shape's own zeros, which coincide with the window edges only at those exact points).
- `qo_infidelity`: `1 - min(real(fidelity(ρ,σ)), 1)`, methods for both `Ket`/`Ket` (wraps into density operators first) and `Operator`/`Operator` pairs; `in_qo_infidelity` maps it over a state vector against one fixed target.
- Parameter-space sampling: `twoD_parameter_space`/`threeD_parameter_space` build a dense grid (`LinRange` per linear dimension, `logrange` for the third dimension in the 3D case) then evaluate a caller-supplied probability function `p` pointwise over it; `..._NN_outputs` then does a weighted `sample(..., Weights(prob), n_samples)` from that grid (`FL_1step`/`FL_1step_3p`), or rejection-samples continuously (`FL_1step_2drives`'s `_NN_outputs`, with optional log-uniform sampling per dimension).
- NN input features, same recipe for every variant: expectation values of the final state on a random orthonormal Hermitian operator basis (`rand_hermitian_orthonormal_basis(d, bases)` — generalizes Gell-Mann matrices to arbitrary dimension via QR-orthogonalized random Hermitian matrices) **concatenated with** the spin-flip-stage infidelity and the full-protocol infidelity vs. target.
- Normalization: separate min-max (`normalize_data`/`denormalize_data`/`max_min`) and standardization (`standardize_data`/`mean_variance`) utilities; `train_test_dataset_normalization` fits min-max stats on the **training set only**, applies to both train/test (an alternate fit-on-everything version is present but commented out).
- Training: `training!` does one `Flux.withgradient`+`Flux.Optimisers.update!` step **per individual sample** (not batched), for `N_epochs` full passes; loss function selected from a `Dict{Symbol,Function}` including two custom ones (`relative_mse`, `mape`) and one bespoke `loss_1` (a weighted sum of `|ŷᵢ-yᵢ|^0.5` over the first two output dims specifically — not general-purpose).
- `execute_problem_NN(dataset_features, NN_features)` is the top-level orchestrator: dataset generation/import (branch on `modality_dataset`) → optional normalization → `train_test_prediction!` (train → test → `prediction_func = D_predictions[NN_features.type_prediction]`, currently only `:prediction_infidelity` registered) → returns `(dataset_vector, predicted_parameters, predicted_final_state, infidelity_prediction)`.

---

## Part 2 — `QuantumDynamics/framework` in detail

### Subsystem construction and caching (`src/subsystems.jl`)

Every `AbstractSubsystem` stores `name::Symbol`, `ω::Float64`, its local `basis`, and `ops::NamedTuple` — computed once, at construction, never recomputed. `ops` being a `NamedTuple` (not individual struct fields) lets each subsystem type expose a different key set while staying type-stable per type. `Qubit(name,ω)`: `SpinBasis(1//2)`, `ops=(σx,σy,σz,σp,σm,n,h0,id)`, `n=(id+σz)/2`, `h0=ω*n`. `HarmonicOscillator(name,ω;nmax=10)`: `FockBasis(nmax)`, `ops=(a,ad,n,h0,id)`, `n=number(basis)` (QuantumOptics's own — the reference convention `Qubit`'s `n` was deliberately aligned to), `h0=ω*n`. `Transmon(name,ω,α;nmax=4)`: same basis type as `HarmonicOscillator` but default `nmax=4` (anharmonicity detunes higher levels increasingly out of relevance), `h0 = ω*n + (α/2)*n*(n-id)` (`α` negative for a physical transmon), `n` still the plain number operator so it participates in excitation-number-conservation arguments identically to `HarmonicOscillator`.

### Composite systems (`src/composite.jl`)

`CompositeSystem(subs...)`: `basis = tensor(...)`; `index::Dict{Symbol,Int}`; `ops::Dict{Symbol,NamedTuple}` built by `embed(basis, index[name], local_op)` for **every** operator of **every** subsystem, eagerly, once. Rationale: embedding (building the tensor-product matrix) is the expensive step, strictly more so than local operator construction — doing it once up front means Hamiltonian builders and, critically, a time-dependent solver's per-step callback never repeat it. Duplicate subsystem names throw `ArgumentError` at construction specifically because every later lookup goes through the plain `Dict` `index`, which would otherwise silently let the later subsystem shadow the earlier one (unreachable by name, but still occupying a Hilbert-space factor) with no error.

**Import gotcha, load-bearing**: `embed`/`ptrace` are owned by `QuantumInterface`, only re-exported by `QuantumOptics`. A bare `using QuantumOptics` followed by a local `function embed(new_sig...) = ...` does **not** add a method to the existing generic function — Julia silently creates a new, separate `embed` local to the defining module, shadowing the real one (`methods(embed)` would show only 1 method instead of the full multi-method function). `QuantumDynamics.jl` fixes this with an explicit `import QuantumOptics: embed, ptrace` before any `include`.

### Hamiltonian recipes (`src/hamiltonians.jl`)

All lab-frame (`bare_hamiltonian(cs) = Σ h0`, so a coupled system's bare energy is always `ωq·nq + ωo·no`, never a rotating-frame-reduced form). Qubit-like coupling dispatch (`_lower`/`_raise`, pattern-matching `getsubsystem(cs,name)`'s type: `σm`/`σp` for `Qubit`, `a`/`ad` for `Transmon`) is shared by `jaynes_cummings`, `rabi`, `tavis_cummings` — works because both operator pairs satisfy the same algebraic relation the conservation arguments need: `[n,lower]=-lower`, `[n,raise]=+raise`. `tavis_cummings` checks `length(g)==length(qubit_names)` when `g` isn't scalar (`ArgumentError` otherwise) specifically because `zip`ping mismatched-length iterables would otherwise silently drop trailing entries. `dispersive_hamiltonian` computes `χ=g²/Δ` from the subsystems' own `ω`s (not caller-supplied) and rejects `Δ=0` (`ArgumentError`) since that's not a degenerate corner of the approximation's validity domain, it's the fully-resonant regime the approximation doesn't apply to at all — left unguarded, `χ` would silently blow up to `Inf`/`NaN`. `quadratic_coupling` (`g(a+ad)²σz`) is `Qubit`-only like `dispersive_hamiltonian` (no `σz` analogue for a multi-level `Transmon`) and deliberately does not conserve excitation number (`(a+ad)²` mixes `a²`/`ad²` terms, Δn=±2) — used as a sanity control against the JC conservation test in `test/runtests.jl`, the same role `rabi`'s non-conservation plays.

`add_time_dependence(H, terms::Pair...; init_time=0.0) = TimeDependentSum(1.0=>H, terms...; init_time)` — deliberately not named `add_drive`, since an added term need not be an external drive; it may be a rotating-frame artifact (a counter-rotating term left over after a frame transformation) with no classical field behind it. Every term's operator must already be resolved (`op(cs,name,key)` or a fixed sum/product of cached ops) — `set_time!` (called every solver step) only re-evaluates scalar coefficient closures and re-forms the weighted sum; it never rebuilds an operator.

### Dissipation (`src/dissipation.jl`)

Three `Dissipator` subtypes, each validating `rate≥0` (and `nth≥0` where applicable) via its **sole inner constructor** — deliberately not an outer `Real`-typed guard method, because a bare `Float64` argument dispatches to a struct's auto-generated default inner constructor ahead of any outer method, so an outer-only guard would silently not run. `_decay_op`/`_gain_op`/`_dephasing_op` are separate dispatch helpers from `_lower`/`_raise` (not reused) specifically because decay/gain/dephasing are physically meaningful for a bare `HarmonicOscillator` too (cavity photon loss, thermal absorption), while `_lower`/`_raise` are documented as strictly "qubit-like." `Gain`'s `nth` has no default (unlike `Decay`'s `nth=0.0`) since a bare `Gain` with no paired `Decay` in mind isn't a meaningful thing to construct on its own — but `nth=0` is allowed (produces a zero jump operator) so a temperature sweep down to `nth=0` never needs a special case. `Dephasing`'s `sqrt(rate/2)` factor (vs. `Decay`'s plain `sqrt(rate*(nth+1))`) is chosen so `rate` is directly the coherence decay rate `1/Tφ` for a qubit (`c=sqrt(rate/2)σz` ⇒ `c†c=(rate/2)I` ⇒ `dρ01/dt=-rate·ρ01`); the same operator form applied to `HarmonicOscillator`/`Transmon` (`sqrt(rate/2)*n`) does **not** have that same clean property since `n²∝I` fails for `n` (kept for API symmetry, not because the rate means one universal thing there).

### Solver wrapper (`src/evolution.jl`)

`evolve` structurally dispatches: presence of `J` selects `master`/`master_dynamic` vs `schroedinger`/`schroedinger_dynamic`; the type of `H` (`AbstractOperator` vs `AbstractTimeDependentOperator`) selects dynamic vs static — no `QuantumDynamics`-level flag. Defaults `alg=Vern9()`, `reltol=1e-8`, `abstol=1e-10` (`QuantumOptics.jl`'s own: `DP5()`,`1e-6`,`1e-8`). Two **distinct** documented adaptive-solver failure modes on a time-dependent `H`, requiring different fixes:
1. **Narrow kick** — a short feature the solver's step size (grown large over a preceding smooth stretch) can jump clean over without any sample point landing inside it; both embedded error-estimate orders then agree (on data that already missed the feature), so the step is silently accepted. Neither tighter tolerance nor higher order fixes this (the comparison never saw the feature); fix is `tstops` (cheapest, needs the timing known in advance), `dtmax` (caps step size everywhere), or fixed `dt` with `adaptive=false` (most expensive, no error-based check left running at all).
2. **Sustained fast oscillation** — a carrier seen by every step, not aliased away, but under-resolved per-cycle by a low-order method, accumulating small per-cycle phase error into an O(1) bias over many cycles. Tighter tolerance and higher order **do** fix this — exactly why `Vern9`/tight tolerance is the default.

Post-solve check: warns (`maxlog=1`, not an error) if final norm² (closed) or trace (open) drifts from 1 by more than `NORMALIZATION_ATOL=1e-4`; a correct solve at defaults typically drifts ~`1e-7`, so this reliably flags under-resolution. A custom `fout` returning something other than a state passes through unchecked.

### Measurement (`src/measurement.jl`)

`condition_on(cs,state,name,projector) → (ρ_cond, prob)` — `prob` returned explicitly (not left for the caller to recompute) because silently dividing by a near-zero `prob` would otherwise produce an uninformative `NaN`/`Inf` `ρ_cond`. `check_fock_cutoff` is `Ket`-only, deliberately: under a bath, real steady-state population near a cutoff boundary is expected physics, not a truncation artifact, and the function can't tell the two apart, so it refuses density operators outright rather than risk a false positive (or a loosened tolerance that then hides genuine truncation). `levels` defaults to 3: **2** is the structural minimum (closes the blind spot where a single top level can sit at exactly zero population for parity reasons under a Δn=2 coupling, regardless of true convergence), **3** is one extra margin on top, not derived from any specific Hamiltonian's leakage pattern — a coupling that leaks by more than 2 levels at a time isn't guaranteed to be caught by the default. Bounded by `levels ≤ (nmax+1)÷2` (`ArgumentError` otherwise) so the checked region can't swallow the whole basis and become vacuously always-convergent on a small `nmax`.

### Persistence (`src/io.jl`)

Stores `subsystems`/`dissipators` (small, fully reconstructible specs) rather than `cs`/`J` (large, 100%-derived from those specs) — same "cache vs. spec" distinction `CompositeSystem` makes internally, just at the persistence layer. Time-independent `H` round-trips exactly (JLD2 handles a plain sparse `Operator` natively); time-dependent `H` only round-trips if its coefficient is a **named** top-level function — an anonymous closure (the pattern every time-dependent example in the repo actually uses) throws `MethodError: ... are not callable` on reload, a loud failure by design, consistent with the rest of the codebase's preference for surfacing problems (`condition_on`'s `prob`, `FockCutoffTooSmall`) over producing a quietly-wrong result. Recommended pattern: keep drive parameters in `params`, leave `H` as `nothing`/a fixed reference operator, and rebuild the real `TimeDependentSum` from `params` after `load_result`. Each field is written as a separate top-level JLD2 key (not one serialized struct blob) plus a `format_version`, so a future schema change can be handled explicitly on load rather than depending on JLD2 correctly reconstructing an evolved struct shape.

---

## Part 3 — Rewrite plan, Step 1 detail (a)–(g)

Elaborates the Step 1 sub-points from `CLAUDE.md`'s Plan section: rewriting the HBAR-qubit problem on `QuantumDynamics`, scoped to the `:FL_1step_3p` variant only.

**Update, this session**: this is no longer purely design-stage — `NNQuantum/HBAR-qubit_problem.jl` (later renamed `FockLadder_problem.jl`, see the update below) now exists and carries (a)–(g) below as either live code or commented-out pseudocode (noted per sub-point). Three supporting files were also added: `NNQuantum/Definition.jl` (physical constants `h`/`hbar`/`kb` only, ported from `old_version/definitions.jl` with its Unicode-minus typo in `hbar`'s exponent fixed to ASCII `-`; deliberately not the QM-side struct builders, since `QuantumDynamics`'s own `Qubit`/`HarmonicOscillator`/`CompositeSystem` supersede that role), `NNQuantum/Project.toml` (environment: `QuantumDynamics`+`QuantumOptics` deps, `QuantumDynamics` resolved locally via `[sources] = {path = "../QuantumDynamics/framework"}` rather than a registry, no `Manifest.toml` committed — same pattern `QuantumDynamics/framework`'s own `examples/*/Project.toml` use), and `NNQuantum/execution.jl` (later renamed `FockLadder_execution.jl`, see the update below; activates/instantiates the environment, `include`s `Definition.jl` then `HBAR-qubit_problem.jl` (later renamed `FockLadder_problem.jl`); scoped far smaller than `old_version/ML_QM_execution.jl` since the NN/dataset half of that file has no counterpart here yet — step 3 hasn't started). **Environment now instantiated and verified working**, separately from this — `julia --project=NNQuantum -e "using Pkg; Pkg.instantiate()"` resolved and precompiled the full dependency tree (`QuantumDynamics` from its local path, `QuantumOptics`, `OrdinaryDiffEqVerner`, `JLD2`, and their transitive deps) against the locally installed Julia 1.12.7, no errors; `NNQuantum/Manifest.toml` now exists. A sanity check (`using QuantumDynamics, QuantumOptics; Qubit(:qubit, 1.0)`) confirmed both packages actually load and a basic constructor works, run locally via shell tool calls on the same machine the repo lives on — nothing remote involved anywhere in this setup. **Update, later this session — the (d) frame gap resolved.** (c)/(d) are now live code, not pseudocode: `Qubit`/`HarmonicOscillator` in (c) are constructed with pre-transformed frequencies (`Δ0`, `0.0`) instead of the bare `ωq`/`ωm`, which lets (d) call `jaynes_cummings(cs,:qubit,:osc,g)` directly rather than hand-building `H0` — see (d)'s own status note below for the derivation. Verified by actually running the file (`julia --project=NNQuantum NNQuantum/execution.jl` (later renamed `FockLadder_execution.jl`), exit 0) and inspecting the result: `H0` builds as a proper `Operator`, and the constructed subsystems report `ω = -15800.0` (`= Δ0`) and `ω = 0.0`, as expected. **Update, still later this session — (f)/(g) done, (e) folded into (g).** All of (a)–(g) are now live code. (e)'s `Ω(t)`/`Δ(t)`/`add_time_dependence` wiring was never given standalone top-level form — `τ_exc`/`ωd`/`τ_SWAP`/`t0` are per-call pulse parameters, not fixed constants, so they were written directly inside (g)'s `FLstep_dynamics_3p` instead (see (e)'s and (g)'s status notes below). Testing this surfaced a real bug: `t -> Ω(t) => op(...)` (no parens) parses as `t -> (Ω(t) => op(...))` in Julia — `->` binds looser than `=>` — not `(t -> Ω(t)) => op(...)`, the `Pair` `add_time_dependence` actually needs; a `MethodError` on first run caught it immediately, fixed by parenthesizing (matching `add_time_dependence`'s own docstring example, which does the same). The full protocol was then verified end-to-end with a test initial state: `FLstep_dynamics_3p` returns a proper `(tspan, states)` trajectory, final `tr(ρ)=1.0` exactly. What's still not done: no `initial_state` is defined at module level (only used ad hoc for testing), and step 2's actual validation run (realistic pulse parameters, `⟨n_qubit⟩`/`⟨n_mech⟩` plots) hasn't started — `execution.jl` (later renamed `FockLadder_execution.jl`) itself only builds `cs`/`H0` on include, it doesn't call `FLstep_dynamics_3p`. **Update, a later session — file renamed, reframed as system setup.** `HBAR-qubit_problem.jl` is renamed `FockLadder_problem.jl` (content unchanged by the rename). All of (a)–(g) in it are, together, the *setup* of the HBAR+qubit system — physical parameters, subsystems/composite system, Hamiltonian, dissipators, and the two-stage protocol runner built on top of that setup — as distinct from step 2's validation work, which lives in `test.jl`.

**a) Scope — `:FL_1step_3p` only.** Of the three variants in `old_version` (`:FL_1step`, `:FL_1step_3p`, `:FL_1step_2drives`), only `:FL_1step_3p` is ported: free drive frequency `ωd` in place of a fixed `cos(Δ0_tilde·t)` carrier, 3 pulse parameters. `:FL_1step` (fixed carrier) and `:FL_1step_2drives` (BSpline-shaped drives, needs `basis_spline` setup) are dropped from scope entirely — not deferred, not planned as follow-ups. `:FL_1step_3p`'s own detuning-correction term (`χ=g²/Δ0`, meant to correct `Δ0_tilde`) is present in `old_version` but commented out and never exercised (`CLAUDE.md` Part 1) — it's dead code in the source being ported from, so it is dropped rather than carried over dormant. Net effect: `Δ0_tilde = Δ0` unconditionally in the ported version; there is no `typeofcorrection` switch to reproduce.

**Status: done.** Written verbatim as the "(a) Scope" comment block at the top of `FockLadder_problem.jl` (renamed from `HBAR-qubit_problem.jl`) — a scope declaration, not a computation, so there's nothing further to make "live."

**b) Unbound parameters.** `N_mech` (mechanical Fock cutoff) and `g` (JC coupling) are left as open parameters for now — `old_version` never bound them to concrete values either (`N_mech` is a `...` placeholder, `g` is commented out in `old_version`'s `HBAR-qubit_problem.jl`). Concrete values (from Chu et al. or otherwise) are chosen later, only when needed to actually run something for step 2 — not part of the step 1 design work itself.

**Status: superseded, done differently.** Both are now bound to concrete Chu et al. values directly in `HBAR-qubit_problem.jl` (`g=258`, `N_mech=5`), alongside the rest of the problem's physical parameters (`ωq=5.9456e6`, `ωm=5.9614e6`, `γm=0.025`, `Teq`, `nthm`, `κ=19`, `κϕ=0.25`, `Δ0=ωq-ωm`) — all live code, not deferred. `Teq`/`nthm` need `kb`/`hbar`, which `QuantumDynamics` itself has no equivalent of anywhere (it only ever consumes a plain numeric `nth`, never derives one from physical constants) — those two constants (plus `h`, unused so far but ported for completeness) now live in the new `Definition.jl`, `include`d at the top of `FockLadder_problem.jl`.

**c) Subsystems and composite system.** `Qubit(:qubit, ωq)` and `HarmonicOscillator(:mech, ωm; nmax=N_mech)`, tensored via `CompositeSystem(qubit, mech)`. Direct use of `QuantumDynamics`'s existing constructors — no new subsystem type needed. Note the qubit number-operator convention (`n=(id+σz)/2`) already matches `old_version`'s `n_qubit = 0.5*(II+zI)`, so this step carries no sign-convention risk.

**Status: done, live.** Real code in `FockLadder_problem.jl`, tested (`julia --project=NNQuantum NNQuantum/FockLadder_execution.jl` (renamed from `execution.jl`) exits 0). Two changes from this text: the mechanical subsystem is named `:osc`, not `:mech` — aligned to `FUNCTION_MAPPING.md`, which uses `:osc` consistently throughout its operator mapping (§1, §2, §4). And, resolving (d)'s frame gap: `Qubit`/`HarmonicOscillator` are constructed with `Δ0`/`0.0`, not the bare `ωq`/`ωm` — see (d)'s status note for why.

**d) Bare + coupling Hamiltonian — provisional, frame question deferred.** `old_version`'s `H0` is written in the frame rotating at `ωm` (mechanical bare term vanishes, qubit bare term collapses to the detuning `Δ0=ωq-ωm`), while `QuantumDynamics`'s Hamiltonian recipes (`bare_hamiltonian`, `jaynes_cummings`) are lab-frame only by design (Part 2 above). For step 1, `H0` is built to structurally mirror `old_version`'s rotating-frame version — `0.5*Δ0*op(cs,:qubit,:σz) + g*(JC coupling terms)` — using `QuantumDynamics`'s cached embedded operators (`op(cs,name,key)`) directly rather than the framework's lab-frame recipe functions, and rather than `old_version`'s `qub_ho` struct fields. This is explicitly a provisional choice to unblock the rest of the pipeline, not a resolution of the lab-frame-vs-rotating-frame design question — that gets its own discussion later.

**Status: done, live — frame question resolved.** The hand-built version sketched above was superseded once the frame question was worked through in full: `H0 = jaynes_cummings(cs, :qubit, :osc, g)`, using the pre-transformed subsystems from (c) — a one-line call, not a hand-assembled operator sum, once the right frequencies are fed in.

Why this is exact, not an approximation: `old_version`'s rotating frame is a *common* rotation `U(t) = exp(iωm·t·(n_q+n_mech))`, applied to both subsystems together at the same frequency. The JC coupling `g*(σp*a + σm*ad)` conserves `n_q+n_mech` (the same commutation fact `_lower`/`_raise`'s docstring in `hamiltonians.jl` already relies on: `[n,lower]=-lower`, `[n,raise]=+raise`), so it commutes with `U`'s generator and comes through the transform with **no** induced time dependence at all — it's identical before and after. Only the bare energies shift, via the interaction-picture formula `H_I = U·H·U' + i·(dU/dt)·U'`: since `U H U' = H` here (everything in `H` commutes with `N=n_q+n_mech` except... no, `H` itself, `ωq·n_q+ωm·n_mech+H_JC`, is left invariant by conjugation because each term individually commutes with `N`), this reduces to `H_I = H - ωm·N = (ωq-ωm)·n_q + H_JC = Δ0·n_q + H_JC` — exactly what constructing `Qubit(:qubit,Δ0)`/`HarmonicOscillator(:osc,0.0)` and calling `jaynes_cummings` produces, since `bare_hamiltonian` sums `ω*n` per subsystem.

The one remaining difference from `old_version`'s own `H0`: `Δ0*n_q = 0.5*Δ0*id + 0.5*Δ0*σz`, so this `H0` carries an extra constant `0.5*Δ0*id` term that `old_version`'s `0.5*Δ0*σz` alone drops. `FUNCTION_MAPPING.md` §2 already flagged this exact term as "physically inert (a global phase never affects observables/fidelities) but not numerically identical if you diff Hamiltonians directly" — confirmed here by the explicit derivation rather than left as an assertion.

This also resolves, for free, a question (f) never had to ask explicitly: `Decay`/`Gain`/`Dephasing`'s jump operators (`a`, `ad`, `σz`, `σm`) all commute with `U`'s generator up to a pure phase too, which drops out of the Lindblad dissipator term `L·ρ·L' - ...` entirely — so (f)'s dissipators need no frame-transforming either, consistent with `old_version` using the same four operators unmodified in its own rotating frame.

Verified by running the file: `H0`'s type checks out as a proper `QuantumOpticsBase.Operator` on the composite basis, and the constructed subsystems' `ω` fields print as `-15800.0` and `0.0` — i.e. `Δ0` and `0.0` respectively, matching `ωq-ωm = 5.9456e6-5.9614e6 = -15800.0` exactly.

This resolves the gap without touching `QuantumDynamics` itself — no new rotating-frame builder was needed, `jaynes_cummings` already does the job once fed the right frequencies. A general-purpose rotating-frame utility inside `QuantumDynamics` (for problems where a *common* rotation doesn't trivialize the coupling term the way it does here) remains a real gap for future problems, just not this one.

**e) Drive terms.** `Ω(t)` (spin-flip stage) and `Δ(t)` (SWAP stage), reusing `old_version`'s `π_pulse_shape` unchanged (pure math, no framework dependency). `Ω(t) = Ω_R·π_pulse_shape(t,t0,τ_exc)·cos(ωd·t)` — `ωd` free instead of fixed to `Δ0_tilde`, per (a). Wired onto `H0` via `add_time_dependence(H0, Ω=>op(cs,:qubit,:σx), Δ=>op(cs,:qubit,:σz))`, replacing `old_version`'s mutated-`LazySum`/closure approach with `QuantumDynamics`'s `TimeDependentSum`.

**Status: folded into (g), done.** `π_pulse_shape` is ported verbatim (`old_version/ML_QM_library.jl:188-196`) as real, standalone, runnable code — it has no dependency on anything unbound, so there was no reason to leave it commented. `Ω(t)`/`Δ(t)`/the `add_time_dependence` wiring never became separate top-level code, even once `cs`/`H0` from (c)/(d) went live: `τ_exc`/`ωd`/`τ_SWAP`/`t0` are per-call pulse parameters (unlike `ωq`/`ωm`/`g`/etc., which are fixed physical constants correctly bound as globals), so they only make sense as arguments to a function — they were written directly inside (g)'s `FLstep_dynamics_3p` instead of being duplicated here as a free-standing demo. One correction from the text above, carried into the actual code: the coefficient passed for the `σz` term is `Δ(t)/2`, not `Δ(t)` — matches `old_version`'s `Ht.factors[2] = Δ(t)/2` and `FUNCTION_MAPPING.md` §3's own worked example, missed in this paragraph's original wording.

**Correctness note, caught only by running the code, not by reading it**: the first live draft of the `add_time_dependence` call — written as `t -> Ω(t) => op(cs,:qubit,:σx)`, unparenthesized — was silently wrong. Julia's `->` has lower precedence than `=>`, so that parses as `t -> (Ω(t) => op(...))`, a single lambda that *returns* a `Pair` each time it's called, not `(t -> Ω(t)) => op(...)`, a `Pair` *of* a function and an operator — which is what `add_time_dependence`'s signature (`Pair{<:Any,<:AbstractOperator}...`) actually requires. `hamiltonians.jl`'s own docstring example parenthesizes the same pattern (`(t -> A*cos(ωd*t)) => op(...)`) for exactly this reason. Running the function raised a `MethodError` immediately; fixed by parenthesizing both terms. Worth remembering as a general Julia gotcha for any future `coeff => operator` pair built from an inline lambda.

**f) Dissipators.** `old_version`'s four fixed jump operators map onto `QuantumDynamics`'s typed dissipators: `thermal_bath(:mech, γm, nthm)` replaces the mechanical decay+gain pair (`sqrt(γm*(nthm+1))*Ia`, `sqrt(γm*nthm)*Iad`) with a single call that keeps the two rates matched by construction; `Dephasing(:qubit, κϕ)` replaces `sqrt(κϕ/2)*zI`; `Decay(:qubit, κ)` replaces `sqrt(κ)*mI`. This sub-step is a near-direct improvement over `old_version` rather than a lossy translation — no open design question here.

**Status: done, live.** `J = jump_operators(cs, [thermal_bath(:osc, γm, nthm)..., Dephasing(:qubit, κϕ), Decay(:qubit, κ)])` (again `:osc`, not `:mech`) in `FockLadder_problem.jl`. `FUNCTION_MAPPING.md` §4 independently confirms all four factors match exactly.

**g) Two-stage protocol runner.** Port `old_version`'s `FLstep_dynamics_3p` orchestration as-is: `evolve` over `[t0, t0+τ_exc]` (spin-flip stage), take the resulting final state, `evolve` over `[t0+τ_exc, t0+τ_exc+τ_SWAP]` (SWAP stage). This is framework-agnostic control flow — translates directly regardless of the open question in (d). `old_version`'s four different return shapes (selected by a `modeofdynamics` runtime symbol) are not carried over as a requirement; what the runner returns is left open until step 2's validation needs are concrete (at minimum, the full `(tspan, states)` trajectory, since that's what gets plotted).

**Status: done, live, tested.** `FLstep_dynamics_3p(t0, initial_state, τ_exc, ωd, τ_SWAP)` in `FockLadder_problem.jl`, returning the concatenated `(tspan, states)` as this paragraph anticipated. `FUNCTION_MAPPING.md` §3, read after this text, raises a consideration not captured above: `QuantumDynamics`'s own examples (`fock_state_preparation`, `resonant_fock_state_preparation`) don't chain two `evolve` calls with manual state hand-off the way this does — they build one coefficient function over the whole schedule and make a single `evolve` call, which is more idiomatic but would need `tstops`/`dtmax` at the stage boundary to avoid the "narrow kick" adaptive-solver failure mode (`τ_exc`/`τ_SWAP` are short relative to the full trajectory). Flagged in-file as an open, undecided alternative — a genuinely separate question from (d)'s (now-resolved) frame gap, about `evolve`-call structure rather than physics — not resolved in favor of either approach yet.

Verified end-to-end with a scratch initial state (`spindown ⊗ fockstate(...,0)`, `τ_exc=τ_SWAP=1e-3`, `ωd=Δ0` — arbitrary placeholders, not physically tuned): returns a 3-point `(tspan, states)` trajectory, final state a proper density `Operator` with `tr(ρ) = 1.0` exactly, and no normalization warning from `evolve`. This exercises the full pipeline — `H0` (d), `add_time_dependence` (e), `J` (f), both `evolve` calls (g) — end to end for the first time. It does *not* mean the physics is validated (that's step 2's job, with parameters actually chosen to be physically sensible, not arbitrary test values); it only confirms the code runs and produces a well-formed, correctly-normalized result.

**Update, a later session — `test.jl` added, step 2's first smoke test.** `NNQuantum/test.jl` is now the project's designated test-script location (see `CLAUDE.md`'s Scope note) — where this kind of validation run belongs, rather than `execution.jl` (later renamed `FockLadder_execution.jl`; stays scoped to activating the environment, defining the problem, and — since a later addition, see Part 4 below — the generic-`N_steps` dataset-generation loop) or a one-off scratch script. It fixes a concrete initial state, `spindown(qubit_basis) ⊗ fockstate(osc_basis, 0)` (`qubit_basis`/`osc_basis` read off `getsubsystem(cs, :qubit).basis`/`getsubsystem(cs, :osc).basis` after `include`ing `FockLadder_problem.jl`), where earlier runs used only an ad hoc scratch state.

It does not call `FLstep_dynamics_3p` directly. That function's per-stage `evolve` call is passed a bare `(t_start, t_end)` tuple as `tspan`, so the solver's returned output — correct for the trajectory itself — only has two samples per stage, far too coarse to plot a curve against. Rather than change `FLstep_dynamics_3p`'s signature (out of scope for a test script to alter), `test.jl` defines its own `run_short_dynamics(t0, initial_state, τ_exc, ωd, τ_SWAP; npoints=200)`, line-for-line the same construction (`H0`, `add_time_dependence` with the identically-parenthesized `(t -> Ω(t)) => op(...)`/`(t -> Δ(t)/2) => op(...)` pairs, `J`, the two sequential `evolve` calls with the stage-1-final-state-feeds-stage-2 hand-off) but with each stage's `tspan` built as `range(t_start, t_end; length=npoints)` instead of a bare 2-tuple, purely for output resolution. This is a test-local convenience, not a change to the pipeline `FockLadder_problem.jl` defines.

A success check gates the plot: `length(states) == length(tspan)`, every returned state is an `AbstractOperator` (expected under `master`/`master_dynamic` dispatch, i.e. `evolve` was correctly routed to the open-system solver given `J` was passed), and the final state's trace drift from 1 is under `1e-3`. Only if all three hold does `test.jl` proceed to plot — `using CairoMakie` (added to `Project.toml` as a new dependency, `Pkg.add`ed conditionally at the top of `test.jl` if not already present, matching the plotting library `QuantumDynamics/framework`'s own examples already use, e.g. `examples/fock_state_preparation/population_ladder.jl`'s `Figure`/`Axis`/`lines!`/`save` pattern), computing `⟨n_qubit⟩`/`⟨n_osc⟩` as `real.(expect(op(cs, :qubit, :n), states))`/`real.(expect(op(cs, :osc, :n), states))`, plotting both on one `Axis` with a vertical dashed line at the stage-1/stage-2 boundary (`t0 + τ_exc`), and saving to `test_plot.png` via `save(joinpath(@__DIR__, "test_plot.png"), fig)`.

First run (`τ_exc=τ_SWAP=1e-3`, `ωd=Δ0` — the same short, arbitrary-but-now-concrete parameters used in (g)'s own verification, not physically tuned): 399 states returned, `tr(ρ_final) = 1.0` exactly, no `evolve` normalization warning, `test_plot.png` produced. This is the first time `NNQuantum`'s bound physical parameters (`Δ0`, `g=258`, the four dissipators) ran through the full two-stage pipeline with a real, non-scratch initial state and produced a plotted, inspectable trajectory — a step 2 smoke test, not yet step 2's actual qualitative-shape validation (full population swap, bath-equilibrium approach), which needs pulse parameters chosen for that purpose rather than this test's short placeholders. First run also cost a one-time ~892s `CairoMakie` precompile (resolving and building its full dependency chain — `Makie`, `GeometryBasics`, `Cairo`, image/font backends, ...) now cached in the local Julia depot; subsequent runs of `test.jl` don't pay this again unless the environment's `Manifest.toml` changes.

---

## Part 4 — `FockLadder_problem.jl` (h): dataset generation, an early step-3 fragment

Not part of the (a)-(g) system setup in Part 3 above, and not itself step 3 (`CLAUDE.md`'s "Translate `old_version`'s ML engine, kept as general as possible" is still open — no struct/dictionary-dispatch design decision has been made here). This is a narrow, explicitly-requested port of exactly two functions plus their dependencies, landed ahead of that broader design work rather than after it.

**Ported, same names, for direct correspondence**: `FL_1step_3p_NN_outputs`/`FL_1step_3p_NN_inputs` from `old_version/HBAR-qubit_problem/HBAR-qubit_problem.jl:325-358` — the `:FL_1step_3p`-specific pair, not the plain `:FL_1step`/`FL_1step_2drives` families (both out of scope per (a)). `FL_1step_3p_NN_outputs(p, parameters_range, dim_parameters_space, n_samples)` weighted-samples `n_samples` `(τ_exc, ωd, τ_SWAP)` triples from a 3D grid, unchanged in structure from `old_version`. `FL_1step_3p_NN_inputs(t0, initial_state, target_spinflip, target_final, decom_basis, pulse_parameters, n_samples)` runs each triple through `FLstep_dynamics_3p` and returns one feature row per sample.

**Signature change from `old_version`, deliberate**: `old_version`'s `..._NN_inputs` takes a single `dataset_features::fl_1step_features` struct (`state_target_spinflip`, `state_target_1step`, `decom_basis`, `phonon_n`, `correction`). This port flattens that to the three fields the `:FL_1step_3p` port actually uses (`target_spinflip`, `target_final`, `decom_basis`) as plain positional arguments — `phonon_n`/`correction` were the detuning-correction knobs already dropped per (a), so carrying the struct forward just to hold two unused fields (plus the two now-plain ones) would be dead weight. Whether some struct comes back, and in what shape, is exactly step 3's open question — not decided here, and not blocked by this choice either way.

**A genuine simplification, not just a port**: `FLstep_dynamics_3p` already returns the concatenated 3-point `(tspan, states)` trajectory — `states[1]` = initial, `states[2]` = spin-flip endpoint, `states[end]` = protocol endpoint — a direct consequence of each stage's `evolve` call being passed a bare 2-point `tspan` (see (g)'s status note above). `old_version`'s `FLstep_dynamics_3p` needed a `modeofdynamics=:dynamics` argument specifically to get `(ρ_at_flip, ρ_at_end)` back in a separate call shape; `FLstep_dynamics_3p` here needs no equivalent — `FL_1step_3p_NN_inputs` just indexes into the one trajectory shape the function already produces. No change to (g) was needed for this.

**Generic helpers, provisionally colocated**: `qo_infidelity`, `rand_hermitian_orthonormal_basis`, `threeD_parameter_space` are ported from `old_version/ML_QM_library.jl` (the problem-agnostic half there) directly into `FockLadder_problem.jl`, since no such library file exists in `NNQuantum` yet. `qo_infidelity` collapses `old_version`'s two separate `Ket`/`Operator` methods (`ML_QM_library.jl:203-213`) into one, via a small `_as_density_operator` dispatch helper — needed because `FLstep_dynamics_3p`'s states are always density operators (`J` is always passed, per (f)), while a caller's target state may still reasonably be given as a plain `Ket`. `rand_hermitian_orthonormal_basis` is ported unchanged (`ML_QM_library.jl:65-97`), generating a `d²`-element orthonormal Hermitian operator basis (a Gell-Mann generalization) via QR on the stacked real/imaginary vectorization of `d²` random Hermitian matrices, `d` = the full composite Hilbert-space dimension (matches `old_version/ML_QM_execution.jl`'s own usage: `dimension` there is documented as "Ket state vector's dimension", i.e. the full composite dimension, not a truncated feature subspace). `threeD_parameter_space` is unchanged, and needs `Base.logrange` (Julia ≥1.11, available on the project's Julia 1.12.7 — no new dependency) for its log-spaced third dimension.

**One deliberate non-port**: `old_version`'s weighted sampling goes through `StatsBase.sample(space, Weights(prob), n)`. Rather than add `StatsBase` as a new project dependency for one call, this port uses a small hand-rolled `weighted_sample` (cumulative-weight + `searchsortedfirst`, sampling with replacement — the same default `StatsBase.sample` uses) — consistent with the project's minimal-dependency stance (`DESIGN.md` Part 2: `QuantumDynamics/framework` itself lists only 3 dependencies "deliberately minimal"; `CairoMakie` was the one exception so far, justified by matching an existing example convention, which doesn't apply here).

**New top-level `using` statements**: `FockLadder_problem.jl` now also does `using QuantumOptics` (for `fidelity`, `expect`, `Ket`, `Operator`, `dm`, `randn`, `qr`'s `ComplexF64` matrix ops) and `using LinearAlgebra` (for `qr` itself) — both already present as environment dependencies (`QuantumOptics` via `Project.toml`, `LinearAlgebra` a stdlib), just not previously `using`d directly in this file (`execution.jl`/`test.jl` both already `using QuantumOptics` before including it, so this makes the file self-contained rather than relying on the includer, matching how `Definition.jl` is `include`d directly rather than assumed pre-loaded).

**Verified working**: a scratch smoke test (tiny parameter ranges, `n_samples=3`, `dim_parameters_space=[5,5,5]`, arbitrary target states, `N_mech=5` so composite dimension `d=12`) confirmed `rand_hermitian_orthonormal_basis(12, cs.basis)` returns 144 (`=d²`) operators, `FL_1step_3p_NN_outputs` returns 3 `(τ_exc, ωd, τ_SWAP)` triples drawn from the grid, and `FL_1step_3p_NN_inputs` returns 3 feature rows of length 146 (`=d²+2`, the `+2` being the two infidelities) each. Not yet exercised: an actual dataset run at realistic sample counts, or wiring this into anything resembling `old_version`'s `dataset_generation`/`execute_problem_NN` orchestration — both squarely step 3's job.

### Dataset persistence (JLD2), a later addition to (h)

`dataset_rows(inputs, outputs)` and `save_dataset(path, inputs, outputs; description="", params=Dict{Symbol,Any}())`, added to the same (h) section once `FL_1step_3p_NN_outputs`/`FL_1step_3p_NN_inputs` above existed to feed it. `dataset_rows` builds one `Vector{Float64}` row per sample as `vcat(inputs[i], outputs[i])` — matches `old_version`'s `dataset_creation` (`ML_QM_library.jl:232-253`) row layout (input columns then output columns) exactly, but as its own function rather than folded into a larger `dataset_generation` orchestrator, since `NNQuantum` has no such orchestrator yet (step 3, still unstarted). `save_dataset` stacks those rows into a matrix (`permutedims(reduce(hcat, rows))`, one sample per matrix row) and writes it to a `.jld2` file.

**Followed an existing convention rather than inventing one**: `QuantumDynamics/framework/src/io.jl`'s `save_result`/`load_result` already establish this project's JLD2 pattern — separate top-level keys (`format_version`, then each field) via `jldsave`, not one serialized struct blob — specifically so a later schema change can fall back on a missing key instead of depending on JLD2 correctly reconstructing an evolved struct shape (see that file's own docstring). `save_dataset` follows the same shape: `format_version` (a new `DATASET_FORMAT_VERSION`, independent of `io.jl`'s `RESULT_FORMAT_VERSION` — different schema, different file layout, no reason to couple their version counters), `dataset` (the stacked matrix), `dim_input`/`dim_output` (so a reload can split `dataset[:, 1:dim_input]`/`dataset[:, dim_input+1:end]` without needing `inputs`/`outputs` themselves still in memory), plus the same free-form `description`/`params` fields `SimulationResult` itself carries, for a human-readable note and structured metadata (e.g. which physical parameters produced this dataset) respectively.

**New dependency, added the same way the environment itself was set up**: `JLD2` was already a *transitive* dependency (pulled in via `QuantumDynamics`), but a direct `using JLD2` inside `NNQuantum`'s own code needs it declared as a *direct* `Project.toml` dependency too — Julia's environment resolution doesn't expose a transitive dependency's exports to code outside the package that declared it. Added via `julia --project=NNQuantum -e "using Pkg; Pkg.add(\"JLD2\")"` (real Julia commands resolving and pinning the exact version, `v0.6.6`, into `Project.toml`/`Manifest.toml` — the same pattern used for the environment's original `Pkg.instantiate()` and for adding `CairoMakie` in `test.jl`, not a hand-typed UUID).

**Verified working**: a 4-sample smoke dataset (same scratch setup as above) was saved, reloaded via `jldopen`, and the reloaded matrix's first row confirmed to equal `vcat(inputs[1], outputs[1])` exactly; `dataset` came back with shape `(4, d²+2+3)` = `(4, 149)` for `d=12`, and `dim_input`/`dim_output` round-tripped as `146`/`3`.

### Dataset-generation smoke test, `test.jl` section 4

Unlike the scratch smoke tests above (real, but never committed as reusable code), this one is written into `test.jl` itself — the project's designated test-script location (`CLAUDE.md`'s Scope note) — as section 4, alongside the existing dynamics/plotting smoke test (sections 1-3). It's a from-scratch reproduction of one specific `Chu_DFL_execution.ipynb` cell, not an ad hoc parameter choice: `old_version/HBAR-qubit_problem/Chu_DFL_execution.ipynb`, the "NN Fock ladder (g = 258)" → "Non-Unitary" cell (id `dfd162e7`), which is the one cell in that notebook using `:FL_1step_3p` + `:master_dynamic` (dissipative) at `g=258` — matching `FockLadder_problem.jl`'s own `g` exactly, so no rescaling was needed to reuse its numbers.

**What was pulled from that cell, restricted to `step=1` of its `for step in 1:N_steps` loop**:
- Target states, from that cell's `creation_step_states(ρ0, step)` (`old_version/HBAR-qubit_problem/HBAR-qubit_problem.jl:241-255`): for `step_number=1`, spin-flip target `spinup(qubit)⊗|step_number-1⟩ = spinup(qubit)⊗|0⟩` (oscillator hasn't gained a phonon yet at the spin-flip midpoint) and SWAP target `spindown(qubit)⊗|step_number⟩ = spindown(qubit)⊗|1⟩` (one phonon added, qubit flipped back down) — one rung of the Fock ladder, which is where the file's new name comes from.
- Parameter ranges: `T_exc_ini,T_exc_fin = 1e-5, 5e-2`; `ωd_ini,ωd_fin = Δ0∓100` (the cell's `δ0∓δ`, `δ0=Δ0`, `δ=100`); `τ_SWAP_ini,τ_SWAP_fin = t_swap_th∓{δ1,δ2}` with `t_swap_th = π/(2g√step_number)`, `δ1=0.0005`, `δ2=0.00015` — all copied unchanged, `step_number=1` substituted in directly rather than looped.
- `dim_parameters_space = [100,100,100]` and `prs(τ_exc,ωd,τ_SWAP) = 1/prod(dim_parameters_space)` — the `:master_dynamic` cell's own grid size and uniform sampling probability (the sibling `:schroedinger_dynamic` cell earlier in the notebook uses `[5e2,1e2,5e2]` instead; the dissipative cell's numbers were used since `FLstep_dynamics_3p` here is always dissipative).
- `SUN_basis = rand_hermitian_orthonormal_basis(dimension, basis)` → `decom_basis = rand_hermitian_orthonormal_basis(d_dataset, cs.basis)`, `d_dataset = length(cs.basis)`, matching the notebook's own `dimension = 2*(N_mech+1)` convention (full composite dimension, not truncated).

**Deliberately not pulled from that cell**: `n_samples=800`/`n_training=750` (this task asked for `n_samples=5`, a smoke test, not a real dataset — no train/test split exists yet, nor a use for one, since step 3 hasn't started); `N_mech=9` (that cell's own value, different from `FockLadder_problem.jl`'s bound `N_mech=5` — left as `FockLadder_problem.jl` already has it, since only the parameter *ranges* were asked for, not a change to the physical setup); the entire NN-training half of the cell (`model`, `NN_features`, `execute_problem_NN`) — dataset generation and saving only, per this task's own scope.

**`t0`/`initial_state` reused from sections 1-3 above**, not redefined: `t0=0.0` and `initial_state = spindown(qubit_basis)⊗fockstate(osc_basis,0)` (a `Ket`) already exist earlier in `test.jl`; `evolve`'s own docstring confirms a `Ket` initial state is accepted and converted automatically for the dissipative (`J`-present) dispatch `FLstep_dynamics_3p` always uses, so no separate density-operator construction was needed here (unlike the notebook's own `ρ0 = ψ0 ⊗ dagger(ψ0)`, built explicitly because `old_version`'s dynamics functions dispatch on `typeofdynamics` and aren't always dissipative).

A success check (`length(inputs_dataset) == n_samples_dataset` and every row's length `== d_dataset²+2`) gates the save, matching section 3's `simulation_ok`-gates-the-plot pattern.

**Run end-to-end** (`julia --project=NNQuantum NNQuantum/test.jl`, the real script, not a scratch copy): sampled 5 `(τ_exc,ωd,τ_SWAP)` triples (e.g. `(0.0237, -15702.0, 0.00610)`), `FL_1step_3p_NN_inputs` returned 5 rows of 146 values each, the success check passed, and `save_dataset` wrote `NNQuantum/dataset_step1.jld2` — reload-verified separately: `dataset` shape `(5,149)`, `dim_input=146`, `dim_output=3`, `params` round-tripping `step=1, g=258, n_samples=5, N_mech=5`, and the first row's last 3 entries matching the sampled pulse-parameter triple exactly. `dataset_step1.jld2` is a generated artifact (like `test_plot.png`), left untracked rather than committed.

---

## Part 5 — `execution.jl` renamed `FockLadder_execution.jl`, generic-`N_steps` dataset generation

`execution.jl` → `FockLadder_execution.jl` (`git mv`, content restructured by the same commit — unlike `HBAR-qubit_problem.jl` → `FockLadder_problem.jl`, this rename came with substantial new content, not a pure rename). It still does exactly what it did before (activate the environment, `include` `Definition.jl`/`FockLadder_problem.jl`) and now also defines `run_Fock_ladder`.

**Why this file, not `test.jl`**: `test.jl`'s own section 4 (Part 4 above) already generates a step-1 dataset, but as a fixed, single-step smoke test — proving the plumbing works, not something meant to be called with an arbitrary `N_steps`. `old_version`'s own split shows the same distinction: `ML_QM_execution.jl` is where the actual (configurable, multi-step) pipeline is assembled and run, while validation happens interactively (the notebook). `FockLadder_execution.jl` is `NNQuantum`'s `ML_QM_execution.jl` counterpart in that sense — `run_Fock_ladder` belongs there, not in the test-script location `CLAUDE.md` reserves for smoke tests.

**`run_Fock_ladder(N_steps; n_samples=800, dim_parameters_space=[100,100,100], save_dir=@__DIR__)`**, structurally a generalization of `test.jl` section 4's `step=1`-only body to `for step in 1:N_steps` — same target-state construction (`spinup(qubit)⊗|step-1⟩` / `spindown(qubit)⊗|step⟩`), same parameter-range construction (`T_exc` fixed across steps; `τ_SWAP` centered on `t_swap_th=π/(2g√step)`, so it narrows as the ladder climbs and the JC swap gets faster — direct port of `Chu_DFL_execution.ipynb`'s own `for step in 1:N_steps` loop, cell `dfd162e7`), same `FL_1step_3p_NN_outputs`/`FL_1step_3p_NN_inputs`/`save_dataset` call sequence — but with `decom_basis` built once before the loop (matching that cell's own `SUN_basis`, built once and reused every step) rather than rebuilt per step, and each step's output saved to its own file, `dataset_step<step>.jld2` (same naming `test.jl`'s smoke test already uses for `step=1`, so a full `run_Fock_ladder` run's `dataset_step1.jld2` and the smoke test's are interchangeable in shape).

**The NN half, left blank as requested**: `old_version`'s `execution_dynamic_FL` (both this cell and its `:schroedinger_dynamic` sibling) trains a NN on each step's dataset, predicts that step's `(τ_exc,ωd,τ_SWAP)`, and — critically — uses the *actual* final state that prediction's own trajectory reaches (`final_state` from `execute_problem_NN`) to seed the next step, advancing `t0` by the predicted pulse durations (`t0 + sum(time)` in the `:master_dynamic` cell). None of that exists in `NNQuantum` yet (step 3, unstarted). The loop body has an explicit comment block marking exactly where that would go, describing what it would need to do once implemented (train on `(inputs,outputs)`, predict, re-run `FLstep_dynamics_3p` with the prediction, carry forward its real final state) — no stub function, no invented interface, since the shape of that step is itself the open question `CLAUDE.md` defers to step 3.

**Placeholder step-advance (original version, since replaced — see the restructure note below)**: without a predicted trajectory to carry forward, `run_Fock_ladder` initially advanced `t0`/`initial_state` using each step's own *target* state (`initial_state = target_final`) and nominal timing (`t0 += mean(T_exc range) + t_swap_th`) — i.e. assumed perfect ladder-climbing. This was explicitly not physically accurate (real pulses sampled in the dataset mostly won't land exactly on target), but was the simplest substitute that let every step still generate its dataset around a sensible, non-degenerate `(t0, initial_state)` rather than every step starting from `t0=0`/the ground state regardless of how far up the ladder it is. Documented in the function's own docstring as provisional, to be replaced once NN prediction exists — not a design decision about what step 3's real advance logic should look like.

**Not called on `include`**: unlike `test.jl` (which runs its smoke test immediately when executed), `FockLadder_execution.jl` only *defines* `run_Fock_ladder` — real `n_samples`/`dim_parameters_space` (matching `old_version`'s own `n_samples=800`, `dim_parameters_space=[100,100,100]` defaults) means minutes of `evolve` calls per step, not something to run unconditionally on every `include`. A commented-out example call sits at the bottom of the file instead, mirroring how the notebook itself only runs `execution_dynamic_FL` from an explicit, separate cell.

**Verified working (original version)**: `run_Fock_ladder(2; n_samples=3, save_dir=tempdir())` — both steps' datasets saved with the expected shape (`(3,149)` each, `d_dataset=12` for `N_mech=5`), and `t0` (recorded in each file's `params`) advanced correctly between steps: `0.0` for step 1, `≈0.0311` for step 2 (`= mean(T_exc range) + t_swap_th(step=1) = 0.025005 + π/(2·258) ≈ 0.02501+0.00609`).

### Restructure, a later session — aligned to the user's actual step-3 algorithm

The placeholder advance above was flagged, in discussion, as not matching the algorithm the user actually has in mind for step 3. That algorithm, stated explicitly:

0. Start from the true ground state (`spindown(qubit) ⊗ fockstate(osc,0)`).
1. Generate a dataset from the current `(t0, initial_state)`, sweeping a spectrum of drive values.
2. Train a NN on that dataset.
3. Use the trained NN to predict the drive parameters that reach the target state.
4. Reiterate 1-3 using the state the *predicted* pulse actually reaches (not the target state) as the next step's initial condition, until `N_steps` rungs are done.

The gap this exposes: the original placeholder's whole design was built around *not having* steps 2-3, so it substituted the target state directly — but that's precisely the shortcut the real algorithm exists to avoid (a predicted, imperfect pulse generally does *not* land exactly on target; chaining from the target anyway silently pretends every step succeeds perfectly). So the placeholder wasn't a rough draft of the right idea, it was a different idea, and is removed rather than adjusted.

`run_Fock_ladder` is now written as five explicit sections matching steps 0-4 above, inside the same per-step loop (parameter-range construction unchanged, per this task's own scope — "we do not care much about parameters and ranges now"):
- **0**: unchanged, still the ground state, set once before the loop.
- **1**: unchanged, still `FL_1step_3p_NN_outputs`/`FL_1step_3p_NN_inputs`/`save_dataset`, live.
- **2**: `model = nothing` — a stub, with the intended call (`train_NN(inputs, outputs)`) left as an adjacent comment rather than a real function, since the NN architecture/training recipe is exactly step 3's open design question (`CLAUDE.md`), not decided here.
- **3**: `predicted_params = nothing` — same treatment (`predict_drive_parameters(model, target_final, decom_basis)` commented, not implemented).
- **4**: guarded on `predicted_params === nothing`: today that's always true, so the loop `@warn`s and `break`s immediately after step 1's dataset save, for every `N_steps`. The real step-4 body (unpack `(τ_exc,ωd,τ_SWAP)` from the prediction, run `FLstep_dynamics_3p(t0, initial_state, ...)`, then `t0 += τ_exc+τ_SWAP; initial_state = states[end]`) is written and live below the guard — correct, exact code, just unreachable until steps 2-3 exist to produce a real `predicted_params`.

Net behavioral change: `run_Fock_ladder(N_steps)` for any `N_steps > 1` now only ever produces `dataset_step1.jld2` (plus a warning), where the previous version would have produced all `N_steps` datasets using the physically-wrong perfect-climb assumption. This is intentional — an honest "not implemented beyond here" is preferred over a placeholder that looks like a full multi-step run but silently isn't one.

---

## Part 6 — Plan for today: the NN engine

Documents the plan for today, closing `run_Fock_ladder`'s steps 2-3 stubs (`train_NN`/`predict_drive_parameters`, Part 5's restructure note), agreed in discussion this session. **Nothing below is implemented yet** — captured ahead of code per an explicit request to update documentation first. Five sub-steps.

**(i) `NNQuantum.jl`, a new NN library file.** Part 4 provisionally colocated `qo_infidelity`/`rand_hermitian_orthonormal_basis`/`threeD_parameter_space` in `FockLadder_problem.jl` "since no library file exists yet" and flagged where these belong as exactly step 3's open question. This sub-step answers it: a dedicated file, following the pattern every other multi-file piece of this project already uses (`Definition.jl` for constants, `QuantumDynamics/framework/src/*.jl` for its own topic-split modules).

**Status: done, live.** `NNQuantum/NNQuantum.jl` now exists, holding exactly the three named functions (plus `qo_infidelity`'s private `_as_density_operator` helper) moved unchanged out of `FockLadder_problem.jl`, with their own `using QuantumOptics`/`using LinearAlgebra`. `weighted_sample` was deliberately *not* moved — Part 4 treats it as a separate case from this "generic helpers" trio (its own paragraph, "One deliberate non-port," not grouped with the three named here), so it stays in `FockLadder_problem.jl` alongside `FL_1step_3p_NN_outputs`/`_inputs`, which are the only callers of either. `FockLadder_problem.jl` now `include("NNQuantum.jl")` right after its `using QuantumDynamics`/`using QuantumOptics`/`using JLD2` line (before (c)'s subsystem construction, since (h) at the bottom needs it) — its own `using LinearAlgebra` was dropped, since `qr` was the only reason it was there and that moved too. Neither `test.jl` nor `FockLadder_execution.jl` needed any change: both only ever `include("FockLadder_problem.jl")` and use `rand_hermitian_orthonormal_basis`/`qo_infidelity` transitively, the same relationship they already had with `Definition.jl`'s constants. Verified by running `FockLadder_execution.jl` end-to-end (exit 0, `cs`/`H0`/`J` build correctly through the new include chain) and by calling `run_Fock_ladder(1; n_samples=3, dim_parameters_space=[3,3,3], save_dir=tempdir())`, which exercises `rand_hermitian_orthonormal_basis` (`decom_basis`) and `qo_infidelity` (inside `FL_1step_3p_NN_inputs`) from their new location — dataset saved, same "stops after step 1" behavior as before (steps 2-3 still stubs).

Left open, per (i)'s own framing above ("not fully fixed — to be confirmed once (i)/(ii) are underway"): whether `dataset_rows`/`save_dataset`/`DATASET_FORMAT_VERSION` (Part 4's persistence addition) also belong in `NNQuantum.jl` — they're not FL_1step_3p-specific either, but weren't named in (i)'s own scope, so they stayed in `FockLadder_problem.jl` for this sub-step; (ii) can revisit this once the generic/specific boundary gets firmed up by actually porting the training machinery.

**Intended generic/specific boundary** (not fully fixed — to be confirmed once (i)/(ii) are underway): `NNQuantum.jl` takes over `old_version/ML_QM_library.jl`'s role — the problem-agnostic half (training loop, normalization, loss-function dispatch, prediction orchestration; Part 1's inventory: `training!`, the `D_predictions` dict, `normalize_data`/`denormalize_data`/`max_min`, `train_test_dataset_normalization`, `execute_problem_NN`). `FockLadder_problem.jl` keeps the problem-specific half — `train_NN`/`predict_drive_parameters` become thin `:FL_1step_3p`-specific wrappers around `NNQuantum.jl`'s generic calls, the same relationship `FL_1step_3p_NN_outputs`/`FL_1step_3p_NN_inputs` already have with `old_version`'s dataset-generation half. `run_Fock_ladder` itself stays in `FockLadder_execution.jl`, unaffected — it's the orchestrating loop, not NN machinery.

Whether `execute_problem_NN`'s struct-based interface (`dataset_features`/`NN_features`, Part 1) is worth porting as-is, versus something more directly shaped around what `run_Fock_ladder` already has in hand (`inputs`/`outputs` matrices from (h), a target state, `decom_basis`), is left open — a question for (ii), not decided here.

**(ii) Port from `old_version/ML_QM_library.jl` into `NNQuantum.jl`, rebased onto this project's data shapes.** "Rebased" here means: the ported functions consume `save_dataset`/`dataset_rows`'s JLD2 row layout (Part 4's addition — input columns then output columns, `dim_input`/`dim_output` recorded) rather than `old_version`'s in-memory `dataset_features`-driven generation, and any re-simulation/scoring step (e.g. what `execute_problem_NN` does with `predicted_parameters` to get `predicted_final_state`/`infidelity_prediction`) calls `FLstep_dynamics_3p` directly rather than going through `old_version`'s `Dict{Symbol,Function}` dynamics dispatch — there is no dispatch layer to rebase onto in `NNQuantum`, `FLstep_dynamics_3p` is simply the one dynamics function.

**Status: done, live.** `NNQuantum.jl` now also has, in a new section below (i)'s three relocated functions: `load_dataset`/`train_test_split` (JLD2-backed, reading `save_dataset`'s own `dim_input`/`dim_output` keys — replaces `old_version`'s in-memory `dataset_generation` split, per the "rebased" note above); `normalization`/`denormalization`/`max_min`/`normalize_data`/`denormalize_data`/`train_test_dataset_normalization` (ported unchanged, min-max only — `old_version`'s own unused standardization utilities weren't ported); `relative_mse`/`mape`/`loss_1` plus a shared `LOSS_FUNCTIONS` dispatch-by-symbol dict (replacing the dict literal `old_version` rebuilt inline in two different functions); `train_model!` (renamed from `training!`, same per-sample `Flux.withgradient`+`Flux.Optimisers.update!` loop, `@showprogress`/`ProgressMeter` dropped rather than added as a new dependency for a progress bar alone); `test_model` (renamed from `testing_model_infidelity` — the old name overpromised, since its own infidelity/re-simulation code was already dead, commented out, in `old_version`); and `predict_output`/`predict_and_score`, the actual "rebased" re-simulation/scoring step. `predict_and_score` takes the dynamics call as a plain function argument (`simulate::predicted_output -> final_state`) instead of `old_version`'s `plot_problem_dictionary` lookup — exactly the "no dispatch layer to rebase onto" resolution the paragraph above anticipated: a caller (e.g. `FockLadder_problem.jl`'s future `predict_drive_parameters` wrapper, (iv), not written yet) passes a closure over `FLstep_dynamics_3p` directly. This is also why `predict_and_score` stays in `NNQuantum.jl` rather than needing to move to the problem file: it never names `FLstep_dynamics_3p` itself, only calls whatever `simulate` it's handed — so the file boundary from (i) holds even though this function's whole purpose is to drive a problem-specific simulation.

One correction made in porting, not a silent behavior change: `old_version`'s loss dict has `:mae => Flux.Losses.mse` (`ML_QM_library.jl:682,726`, in both places it's rebuilt) — `:mse` and `:mae` both pointed at MSE there, so `:mae` was never actually exercised as MAE anywhere `old_version` used it. Fixed to `Flux.Losses.mae` here, flagged in-file, rather than carried forward silently.

Not ported, either superseded elsewhere in `NNQuantum` or dead/unused in `old_version` itself (documented in `NNQuantum.jl`'s own header comment for this section): `gellmann_operators`/`pauli_operators`; the `PlotlyJS`-based plotting functions and `trajectory_plot` (superseded by `test.jl`'s `CairoMakie` convention); CSV-based `saving_dataset`/`importing_dataset`/`save_matrices_basis` (superseded by `save_dataset`'s JLD2 convention, Part 4); `standardize_data`/`mean_variance`/`dataset_variance` (`old_version` itself never calls these — `train_test_dataset_normalization` only ever used min-max); the BSpline machinery (`:FL_1step_2drives` dropped per (a)); `execute_problem_NN`/`dataset_generation`/`dataset_creation`/`dataset_problems_dictionary` (the `Dict{Symbol,Function}` problem-registration layer itself — there is exactly one problem and one dynamics function in `NNQuantum`, so this layer isn't rebased, it's dropped, replaced by `predict_and_score`'s plain-function-argument design for its one live use).

Verified end-to-end with a scratch smoke test (not written into `test.jl` — that's (iii)'s own job): generated an 8-sample dataset via the existing `FL_1step_3p_NN_outputs`/`_inputs`/`save_dataset` (tiny `dim_parameters_space=[3,3,3]`), reloaded it with the new `load_dataset` (shapes round-tripped, `(8,146)`/`(8,3)`), split 6/2 via `train_test_split`, normalized via `train_test_dataset_normalization`, trained a tiny 2-layer `Flux.Chain` for 3 epochs with `train_model!` (`:mse`, losses finite and decreasing), scored it with `test_model`, then called `predict_and_score` with `simulate` a closure over the real `FLstep_dynamics_3p` (clamping the untrained model's raw prediction into the sampled parameter ranges) — returned a well-formed predicted-parameter vector, a proper final density operator, and an infidelity in `[0,1]` (`≈0.986`, as expected from an untrained model on 8 samples — not a claim of accuracy, just that the full pipeline runs and produces well-formed output). Also re-ran `FockLadder_execution.jl` and `test.jl` in full afterward (both exit 0, `test.jl`'s own dataset-generation smoke test unaffected) to confirm nothing in the existing pipeline regressed from adding `Flux`/`JLD2`/`Statistics` to `NNQuantum.jl`'s own `using` list.

**Status update, later this session — `train_model!`/`test_model` revised from the straight per-sample port to genuine mini-batch training, a deliberate behavior change, not a refactor.** The paragraph above describes the *original* port: one `Flux.withgradient`+`Flux.Optimisers.update!` step per individual sample, `old_version`'s own unbatched shape, carried over as-is. This was since revised, flagged explicitly in `NNQuantum.jl`'s own comment above `train_model!` rather than changed quietly, for two reasons. (1) At the real scale this project's hyperparameters target (`n_samples=800`, `epochs=350`, per `Chu_DFL_execution.ipynb`), the per-sample loop is ~280,000 individual gradient steps per Fock-ladder step — `Flux`/`Zygote` are built around batched matrix operations, so batching here is standard practice at this scale, not a micro-optimization. (2) `old_version`'s per-sample loop also never shuffled row order between epochs (`zip(eachrow(train_X), eachrow(train_Y))`, identical order every epoch) — a real training-quality gap independent of batching, fixed here as a `shuffle(1:n)` permutation recomputed every epoch (`using Random` added to `NNQuantum.jl`'s own `using` list for this).

Concretely: `train_model!` gained a `batch_size::Int=32` keyword (clamped to the training-set size, so a small smoke-test dataset degrades gracefully to full-batch rather than erroring), builds `Xb`/`Yb` as `dim × batch` matrices per chunk of a shuffled permutation, and accumulates `epoch_loss` as a batch-size-weighted mean so a shorter final batch doesn't skew the reported per-epoch loss. `test_model` was changed to match — one batched forward pass over the whole test set rather than a per-row loop — numerically identical to averaging per-sample losses for every loss function actually used here (all plain `mean(...)` calls), just without the per-row overhead; `loss_1`/`:enhancing1` is the one loss that isn't batch-safe, but it isn't used live anywhere in this project (flagged in `NNQuantum.jl`'s own loss-dispatch section).

Also generalized in the same pass: `train_model!`/`test_model` now take `loss::Union{Function,Symbol}` directly — a plain `Function` (e.g. `Flux.Losses.mse` or a caller's own) is used as-is, a `Symbol` is resolved via a new `resolve_loss`/`LOSS_FUNCTIONS` lookup — rather than requiring every loss to be pre-registered in `LOSS_FUNCTIONS` under a `Symbol` key. This follows the same "black box over `Dict{Symbol,Function}` dispatch" reasoning `predict_and_score`'s `simulate` argument already uses (this section's own opening note): `LOSS_FUNCTIONS` stays only as an optional convenience for naming a loss the way `old_version` did (`:mse`, `:mae`, ...), not a hard requirement. `train_and_test_NN` (the (iv) orchestrator below) also gained a matching `batch_size` keyword, threaded straight through to `train_model!`, plus optional `model`/`opt_state` keywords: if both are given, `train_and_test_NN` uses them as-is instead of calling `build_default_model`, so a problem whose input/output dimensions fit the generic dataset shape but whose architecture needs (depth, width, activation) don't match the default gets a way to use the same orchestrator without being forced through `build_default_model`'s fixed choice; `hidden`/`η` are ignored when a model/opt_state pair is supplied, and the two must be given together (an `opt_state` is only valid for the model it was built from) — enforced with an `ArgumentError` otherwise.

Verified by re-running the same scratch smoke test above after the revision: training losses still finite and decreasing over the shuffled mini-batches, `test_model`'s batched error matches the per-sample version to numerical precision on the same data, and `run_Fock_ladder`'s own end-to-end run (see (iv) below) exercises `batch_size`'s default end to end. `FockLadder_execution.jl`/`test.jl` both still exit 0.

**New dependency: `Flux`.** Unlike `JLD2`/`CairoMakie` (small, justified additions to an otherwise minimal `Project.toml` — see Part 2's note on `QuantumDynamics/framework` being "deliberately minimal," 3 deps), `Flux` pulls in its own substantial dependency tree (`Zygote`, `Optimisers`, and their transitives) — closer in weight to what made `old_version`'s single environment carry ~24 deps (Part 1) than to anything added so far in `NNQuantum`. Flagged explicitly rather than added quietly; to be added the same verified way every prior dependency was (`Pkg.add` as a real command, not a hand-edited `Project.toml` entry).

**Status: done.** `julia --project=NNQuantum -e "using Pkg; Pkg.add(\"Flux\")"` — resolved `Flux v0.16.11` (confirms the explicit `Flux.setup`/`Optimisers.update!` API `train_model!` uses is the current one, not a deprecated implicit-`Flux.params` style) plus its transitive tree (`Zygote`, `Optimisers`, `NNlib`, ...), added to `Project.toml`/`Manifest.toml`, same real-command pattern as `JLD2`/`CairoMakie`.

**(iii) Smoke test, in `test.jl`.** Same convention as sections 1-4 already there (Parts 3-4 above): small `n_samples`/`dim_parameters_space`, a handful of training epochs, checks that shapes/success conditions hold and the pipeline runs end-to-end — not a claim of physically meaningful predictions, the same distinction `test.jl`'s existing sections already draw between "runs correctly" and "validated" (Part 3's closing note).

**(iv) Wire the NN half into `run_Fock_ladder`.** Fills in Part 5's restructure-note stubs: step 2 (`model = train_NN(inputs, outputs)`) and step 3 (`predicted_params = predict_drive_parameters(model, target_final, decom_basis)`), replacing the `= nothing` placeholders and the `predicted_params === nothing` guard's `@warn`+`break`. Structure to follow `Chu_DFL_execution.ipynb`'s `execution_dynamic_FL` NN half specifically (the same cell Parts 4-5 already ported the dataset-generation half from) — not a new orchestration invented from scratch, consistent with how every other piece of `NNQuantum` so far has either ported or structurally mirrored a corresponding `old_version` piece rather than redesigning from zero.

**Status: done, live.** Closing this stub exposed that `NNQuantum.jl`'s (ii) machinery, while already data-shape-agnostic, wasn't quite a complete black box yet — a `train_NN`/`predict_drive_parameters` pair genuinely "thin" enough to satisfy (i)'s intended split needed two more generic pieces that didn't exist: a default model architecture and an end-to-end train+test orchestrator tying (ii)'s pieces together. Both were added to `NNQuantum.jl` as part of closing this sub-step, not deferred:

- `build_default_model(dim_input, dim_output; hidden=500, η=1e-4)` — `Flux.f64(Chain(Dense(dim_input,hidden,relu), Dense(hidden,dim_output), softplus))` plus `Flux.setup(Adam(η), model)`, ported from `Chu_DFL_execution.ipynb`'s `:FL_1step_3p`/`:master_dynamic` NN cell (`dfd162e7`) unchanged. A function of `dim_input`/`dim_output` alone — it never sees a pulse parameter or any other problem-specific quantity, so it's reusable for any problem's `train_NN` wrapper regardless of that problem's own input/output dimensionality.
- `train_and_test_NN(X, Y, n_training; hidden, η, epochs, loss, batch_size, model, opt_state)` — bundles `train_test_split`/`train_test_dataset_normalization`/`build_default_model`/`train_model!`/`test_model` into the one call a thin wrapper needs, returning a `NamedTuple` (`model`, `losses`, `test_error`, `test_predictions`, the four normalization stats) so a caller carries one self-documenting bundle downstream instead of eight positional values. `loss` accepts a `Function` or a `Symbol` (resolved via `LOSS_FUNCTIONS`, see the mini-batch status update above); `batch_size` (default `32`) threads through to `train_model!`; `model`/`opt_state`, if both supplied, are used as-is instead of calling `build_default_model` — see the same status update for why.

**The black-box boundary, stated explicitly per this session's discussion**: `NNQuantum.jl` must work for (1) any quantum problem, not just the HBAR-qubit one, and (2) any drive/output parameterization, not just `(τ_exc,ωd,τ_SWAP)` — e.g. `:FL_1step_2drives`'s BSpline coefficients, were that variant ever ported, or an entirely different problem's own output shape. Concretely, this means every function added in (ii)/(iv) operates only on plain matrices/vectors, a `model`, and (for `predict_and_score`) a caller-supplied `simulate` function value — none of it references `τ_exc`, `ωd`, `decom_basis`, or `FLstep_dynamics_3p` by name anywhere. The two problem-specific wrappers that *do* know those things, `train_NN`/`predict_drive_parameters`, now live in `FockLadder_problem.jl`:

- `train_NN(inputs, outputs; train_fraction=0.9375, kwargs...)` — converts `FL_1step_3p_NN_outputs`/`_inputs`'s own return shapes (`inputs::Vector{Vector{Float64}}`, `outputs::Vector{NTuple{3,Float64}}` — the latter tuples, not vectors, since `weighted_sample` draws straight out of `threeD_parameter_space`'s tuple grid; caught by testing, see below) into two plain `Float64` matrices, computes `n_training` from `train_fraction` (clamped to leave at least one sample on each side, so a small smoke-test `n_samples` never produces an empty split), and delegates entirely to `train_and_test_NN`.
- `predict_drive_parameters(nn, t0, initial_state, target_final, decom_basis)` — builds the one input `predict_and_score` needs that it can't supply itself: the target state's own feature vector (`decom_basis` expectation values concatenated with `[0,0]`, matching `Chu_DFL_execution.ipynb`'s `target_input_1step` and every training row's own layout — a state's infidelity with itself is `0`), and a `simulate` closure over `FLstep_dynamics_3p`/`t0`/`initial_state`. `nn` is `train_NN`'s own returned bundle, passed straight through.

`run_Fock_ladder`'s step 4 got simpler as a direct consequence of this design, not as a separate change: `predict_and_score` already re-simulates the predicted pulse to score it by infidelity, so its `final_state` *is* the state a real, imperfect prediction reaches — step 4 just carries that forward as next step's `initial_state` directly, with no second `FLstep_dynamics_3p` call over the same parameters (the way step 4 was written before this session's restructure, and before `predict_drive_parameters` existed to supply that state as a side effect of prediction). `t0 += τ_exc_pred + τ_SWAP_pred` (not `ωd_pred` — `old_version`'s own `t0 += sum(time)` in this cell sums all three predicted values including the drive frequency, which looks like a real, never-caught bug there per `CLAUDE.md` Part 1's "never run with concrete numbers" note; this project's own step 4 already omitted `ωd_pred` from the very first restructure, before this session, so no change was needed here). `run_Fock_ladder` gained six new keyword arguments (`train_fraction`, `hidden`, `η`, `epochs`, `loss`, `batch_size`) that pass straight through to `train_NN`, defaulting to `Chu_DFL_execution.ipynb`'s own cell values (`η=1e-4`, `epochs=350`/`N_epochs`, `:mse`) so a real run matches that cell's hyperparameters by default while staying overridable for cheap smoke-test-scale runs. `batch_size` (default `32`) has no `Chu_DFL_execution.ipynb` equivalent — the notebook's own cell never batches — so it defaults to whatever `train_model!` itself defaults to; see the mini-batch status update earlier in this Part for why batching was added on top of the port in the first place.

**Bug caught by testing, not review**: the first live draft of `train_NN` built `Y` via `permutedims(reduce(hcat, outputs))`, mirroring how `X` is built from `inputs`. `inputs` really is `Vector{Vector{Float64}}`, so that pattern is correct there — but `outputs` is `Vector{NTuple{3,Float64}}` (tuples, not vectors), and `hcat` on a vector of tuples does not splat each tuple into a column; it produces a `1×n` matrix whose elements are whole tuples. `train_test_dataset_normalization`'s `normalize_data` then tried to broadcast per-column stats (length-3 vectors) against that malformed `1×n` array and raised a `DimensionMismatch` on the very first `run_Fock_ladder` smoke run — caught immediately, not by inspection. Fixed with `collect.(outputs)` before `reduce(hcat, ...)`, turning each tuple into a plain vector first.

Verified end-to-end: `run_Fock_ladder(2; n_samples=6, dim_parameters_space=[3,3,3], epochs=3, save_dir=tempdir())` completed both steps in full — no guard, no early stop — printing well-formed test errors, predicted `(τ_exc,ωd,τ_SWAP)` triples inside the sampled ranges, and infidelities in `[0,1]` for each step (this is (v)'s "confirms the full 0-4 loop actually completes" check, informally; (v)'s own formal staged validation is still open, see below). `FockLadder_execution.jl` (include-only, no call) and `test.jl` were both re-run afterward and still exit 0 — nothing in the existing pipeline regressed.

**(v) Staged validation, once (iv) makes step 4 reachable.** First `run_Fock_ladder` restricted to a single Fock step (`N_steps=1`), confirming the full 0-4 loop (dataset → train → predict → advance) completes without the guard tripping — a different, stronger check than Part 5's own "verified working (original version)" run, which only ever exercised the placeholder's fake advance, never a real predicted one. Then, only if that's not already sufficient signal, `N_steps=2`, checking the second step's dataset is generated around the *actual* (imperfect) state the first step's predicted pulse reached — the entire point of the restructure in Part 5, and not something a single-step run can exercise on its own.

---

## Part 7 — Two optimizations to functions ported into `NNQuantum.jl`, a later session

Raised as a discussion question ("does any ported function need optimizing or refining?") and then implemented on request. Both fix genuine inefficiencies carried over unexamined from `old_version` — neither is a correctness bug, and neither changes what either function is mathematically supposed to compute.

**1. `rand_hermitian_orthonormal_basis`: materialize `Q` once instead of indexing it in a loop.** `Q, _ = qr(vecs)` returns `Q` as `LinearAlgebra`'s lazy `AbstractQ` wrapper (a Householder-reflector representation, not a plain matrix). The function's own loop then indexed into it `n = d²` times (`Q[1:half, i]`, `Q[half+1:end, i]`, once per basis operator) — each such access re-derives the requested column from the reflectors rather than reading a materialized array, an avoidable cost repeated `d²` times for no benefit, inherited unchanged from `old_version/ML_QM_library.jl:65-97`'s own loop. Fixed with one line, `Q = Matrix(Q)`, right after the `qr` call — everything downstream is unchanged, since `Matrix(Q)` supports the exact same indexing the loop already does.

**Verified**: for `d=6` (`n=36` operators), the fixed version still returns 36 Hermitian operators (checked via `data ≈ data'`) forming an orthonormal set under the trace inner product (Gram matrix within `~1e-15` of `I`) — i.e. mathematically identical output to before, just computed without the repeated lazy-`Q` indexing.

**2. `FL_1step_3p_NN_outputs`: skip the dense grid entirely when the sampling weight is uniform.** The default path (`threeD_parameter_space` + `weighted_sample`) builds the *entire* `dim_parameters_space` grid (`100×100×100` = 1e6 points at `run_Fock_ladder`'s own default) and evaluates the probability function over all of it, just to weighted-sample `n_samples` points out of that grid — `O(∏dim_parameters_space)` work regardless of how small `n_samples` actually is. Every live caller in this project passes a `p` that is a literal constant (`prs(τ_exc,ωd,τ_SWAP) = 1/prod(dim_parameters_space)`, independent of the point) — both `run_Fock_ladder`'s own `prs` and `test.jl` section 4's — so the grid, and the weighting it exists to support, was never actually doing anything but uniform sampling, at `O(∏dims)` cost instead of `O(n_samples)`.

Added `uniform_parameter_sample(parameters_range, n_samples)` to `NNQuantum.jl`, next to `threeD_parameter_space`: direct continuous sampling within the box, log-uniform in the third dimension (`lo·(hi/lo)^u`, `u~Uniform(0,1)`, the continuous analogue of `threeD_parameter_space`'s own `Base.logrange` spacing for that axis) — `O(n_samples)`, and finer-grained than the grid path besides (continuous, not quantized to `dim_parameters_space`'s resolution). `FL_1step_3p_NN_outputs` gained a `uniform::Bool=false` keyword (default `false` — unchanged behavior for any caller not opting in) that, when `true`, calls this directly and skips `threeD_parameter_space`/`weighted_sample` altogether; `p`/`dim_parameters_space` still have to be passed (unused in that branch) rather than changing the function's signature.

`run_Fock_ladder` gained a matching `uniform::Bool=true` keyword — defaulted to `true`, not `false`, because there is no live path in `run_Fock_ladder` where the sampling is actually non-uniform (`prs` is always the same hardcoded constant), so continuous sampling is strictly the better default there; `dim_parameters_space` stays a real, effective keyword for anyone who passes `uniform=false` explicitly, reverting to the original grid-based behavior. `test.jl` section 4's own call was updated to pass `uniform=true` directly (a fixed smoke-test script, no need for its own keyword).

**Verified**: `run_Fock_ladder(2; n_samples=6, dim_parameters_space=[3,3,3], epochs=3, save_dir=tempdir())` completed both steps in full under the new default (`uniform=true`), producing well-formed continuous `(τ_exc,ωd,τ_SWAP)` predictions and infidelities — no behavioral regression from Part 6 (iv)'s own verification run. `test.jl` re-run end-to-end: both the dynamics/plotting smoke test and the dataset-generation smoke test (section 4) still succeed, now sampling continuous rather than grid-quantized parameter triples.

---

## Part 8 — `run_Fock_ladder` can now reuse a dataset or a trained NN, not just generate/train fresh every step

Before this, `run_Fock_ladder` always did the same thing for every step: make a new dataset, train a brand-new NN on it, predict, move on. This part adds a way to skip some of that — load a dataset made earlier instead of generating a new one, or load an NN that was already trained instead of training from scratch.

**Two new, separate choices, one per step:**

- `dataset_mode` — what to do about the dataset for a step:
  - `:generate_and_save` (the old, and still default, behavior) — make a new dataset and save it.
  - `:generate_only` — make a new dataset but don't save it to disk.
  - `:load` — don't make a new dataset at all; read one that was saved earlier, from a path given in `dataset_paths`.
- `nn_mode` — what to do about the NN for a step:
  - `:train_new` (the old, and still default, behavior) — train a brand-new NN.
  - `:continue_training` — load an already-trained NN from `nn_paths`, and keep training it on the current dataset.
  - `:fixed` — load an already-trained NN from `nn_paths`, and use it to predict right away, with **no training and no dataset at all** — since nothing would use that dataset, we don't waste time making one.

Both `dataset_mode` and `nn_mode` can be one value for all steps, or a list with one value per step (checked to have the right length up front, before anything runs).

**Three small pieces had to be added or changed to make this work:**

1. **Saving and loading a trained NN is new** — `NNQuantum.jl` gained `save_nn`/`load_nn`. They save the model's weights (using Flux's own official, version-safe way — `Flux.state`), the model's shape (`dim_input`/`dim_output`/`hidden`), and the four normalization numbers a prediction needs (without the exact same normalization, a reloaded model's predictions would be wrong). The optimizer's internal state (Adam's running averages) is *not* saved — `:continue_training` just starts a fresh optimizer on the loaded weights, the same way normal fine-tuning usually works; trying to save and restore the optimizer's exact internal numbers would add real complexity for something not actually needed here.

2. **`train_and_test_NN`'s returned bundle got three more fields** — `opt_state`, `dim_input`, `dim_output` (plus `hidden`/`η`, already passed in) — so that a bundle coming from a fresh training run and one coming from `load_nn` look the same, and `save_nn` can be called on either without extra plumbing.

3. **`train_NN` gained a second method** (same name, different input types — plain Julia multiple dispatch) — one for the vector-shaped data `FL_1step_3p_NN_outputs`/`_inputs` produce, one for plain matrices (what `load_dataset` returns). The vector version just converts to matrices and calls the matrix version, so a step using `dataset_mode=:load` and a step generating fresh data both end up going through the same training code.

**A real bug found by testing, not by reading the code**: the first working version of this feature was wrong in a way that didn't show any error — it silently gave meaningless predictions. The cause: `decom_basis` (the random set of operators used to turn a quantum state into a list of numbers a NN can use) is built fresh, with no fixed random seed, every time `run_Fock_ladder` is called. Inside one call, that's fine — the same basis is used for every step of that call. But a dataset or NN saved in one call is only meaningful together with *that call's own* basis. When a *different* call reloaded that dataset/NN, it built its own, different, random basis — so the numbers going into the model at load time didn't mean the same thing as the numbers it was trained on, and the predictions coming out were just wrong, with nothing warning about it.

**The fix**: `NNQuantum.jl` gained `save_operator_basis`/`load_operator_basis` (right next to `rand_hermitian_orthonormal_basis`, which builds this basis in the first place). `run_Fock_ladder` now always saves whichever basis it used to `save_dir/decom_basis.jld2`, and takes a new `decom_basis_path` argument to load one back instead of making a new one. If any step tries to reuse a dataset (`dataset_mode=:load`) or an NN (`nn_mode ∈ (:continue_training,:fixed)`) without also giving `decom_basis_path`, `run_Fock_ladder` now stops immediately with a clear error, instead of running with a silently wrong basis.

**Verified**: ran a short chain of calls sharing one `save_dir` — (1) a fresh run that trains and saves a dataset, an NN, and a basis; (2) the same NN loaded with `:fixed` mode (no training) and the saved basis, run twice — both times gave *exactly* the same prediction, as expected for the same model and the same input; (3) the same dataset and NN loaded with `dataset_mode=:load`/`nn_mode=:continue_training` and the saved basis — trained further without error; (4) the same NN reused *without* passing `decom_basis_path` — correctly stopped with the new `ArgumentError` instead of running. `test.jl` and a plain `run_Fock_ladder` call with the old defaults were both re-run afterward and still work exactly as before.

---

## Part 9 — Made basis-making its own step, and files carry a number showing which basis they belong to

Part 8's fix worked, but it still let `run_Fock_ladder` quietly make a new basis by itself whenever no `decom_basis_path` was given — a caller had to remember, every time, to explicitly pass a path when they wanted to reuse something. The user asked for something safer: make a basis in its own separate step, on purpose, give it a small number, and put that number at the end of every file that depends on it — the basis file, and every dataset/NN made from it. That way you just need to remember one number, and the file names themselves show what goes together.

**New function: `generate_decom_basis(basis_id::Integer; save_dir=@__DIR__, overwrite=false)`.** Makes a new random basis (same `rand_hermitian_orthonormal_basis` call as before) and saves it as `save_dir/decom_basis_b<basis_id>.jld2`. You call this once, by hand, whenever you actually want a new basis — not something that happens automatically anymore. It refuses to overwrite an existing file for the same `basis_id` unless you pass `overwrite=true`, since datasets/NNs may already depend on that file.

**`run_Fock_ladder` changed in three ways:**

1. It takes a new, required second argument, `basis_id`. It no longer makes a basis by itself at all — it only loads `save_dir/decom_basis_b<basis_id>.jld2`. If that file isn't there, it stops right away with a message telling you to call `generate_decom_basis` first, instead of quietly making a new (and wrong, for reuse purposes) one.
2. Every file it saves now ends with the same number: `dataset_step<n>_b<basis_id>.jld2`, `nn_step<n>_b<basis_id>.jld2` (the basis file itself is `decom_basis_b<basis_id>.jld2`, made separately). So all three kinds of file for one basis end the same way, and a mismatch would be visible just by looking at file names.
3. `dataset_paths`/`nn_paths` (from Part 8) still exist, but are now optional overrides, not required. By default, `dataset_mode=:load`/`nn_mode ∈ (:continue_training,:fixed)` just read the file name that `basis_id`/`step`/`save_dir` already point to — you only need to give an explicit path for the unusual case of loading from somewhere else.

**Verified**: (1) calling `run_Fock_ladder` with a `basis_id` that has no saved basis yet correctly stops with a clear error; (2) `generate_decom_basis(1)` makes `decom_basis_b1.jld2`, and calling it again without `overwrite=true` correctly refuses; (3) a fresh run with `basis_id=1` saves `dataset_step1_b1.jld2` and `nn_step1_b1.jld2`, matching the basis's own number; (4) `nn_mode=:fixed` with no explicit `nn_paths` correctly finds `nn_step1_b1.jld2` on its own and gives the exact same prediction as the run that trained it; (5) `dataset_mode=:load`/`nn_mode=:continue_training` with no explicit paths correctly find and reuse the same step's dataset/NN files. `test.jl` was re-run afterward and still works — it never called `run_Fock_ladder`, so it isn't affected by this change.

---

## Part 10 — A plain user guide for `NNQuantum.jl`

Everything above (this whole `DESIGN.md`, and `CLAUDE.md`) is a working history — it explains *why* things are the way they are, in the order they happened, including plans that changed and bugs that got fixed. That is useful for continuing the work, but it is not a good way to just *use* `NNQuantum.jl` — someone wanting to know "what does `train_and_test_NN` take as input" has to dig through a lot of history to find that one answer.

`NNQuantum_API.md` was added to fix that: a plain, function-by-function reference — one line per function in an overview table, then a short, plain-English section for each one (what it does, its inputs, its output, and anything important to know before calling it), grouped the same way the file itself is grouped (comparing states, building a basis, sampling parameters, datasets, normalization, loss functions, training a model, predicting, saving/loading a model). It ends with a short worked example, and a note on how someone would plug in a *different* quantum problem without changing `NNQuantum.jl` itself.

Nothing about `NNQuantum.jl`'s actual code changed for this — it is a new file, `NNQuantum_API.md`, describing the code as it exists after Part 9 above.

---

## Part 11 — Two prerequisites for (v)'s staged validation: dataset save/reopen (already there) and trajectory plotting (new)

Before starting (v) (Part 6's staged validation of `run_Fock_ladder`), the user asked for two things to be in place: a way to save and reopen datasets, and a way to plot a predicted trajectory's expectation values for chosen observables — with a new standing rule that any new function landing in `NNQuantum.jl` gets `NNQuantum_API.md` updated alongside it.

**Dataset save/reopen: checked, already there, nothing added.** `save_dataset` (`FockLadder_problem.jl`, Part 4) and `load_dataset` (`NNQuantum.jl`, Part 6 (ii)) already form a working round trip, and it's not just claimed — `run_Fock_ladder`'s `dataset_mode=:load` (Part 8) already exercises it in practice, on every step that reuses a dataset. Nothing new was needed for this half of the request.

**Trajectory plotting: genuinely missing, added as `plot_trajectory` in `NNQuantum.jl`.** The only existing precedent was `test.jl` section 3's own plotting block — real, working code, but hardcoded to `⟨n_qubit⟩`/`⟨n_osc⟩` specifically and never factored into a callable function, so nothing in the project could plot an arbitrary observable, or plot anything at all outside `test.jl`'s own fixed smoke-test run. `plot_trajectory(tspan, states, observables; title, xlabel, ylabel, vlines, save_path)` takes `observables` as a `Vector` of `label => operator` pairs (fixing both the labeling and the plotted order from the caller's own list), plots each as an expectation-value curve via `real.(expect(operator, states))`, and returns `(fig, values)` — the `CairoMakie.Figure` plus a `Dict{String,Vector{Float64}}` of the raw series, so a caller doesn't have to re-plot just to get the numbers back out. It only ever touches `tspan`/`states`/`observables` as plain data — no reference to `τ_exc`, `decom_basis`, or `FLstep_dynamics_3p` — so it holds the same "problem-agnostic" boundary every other `NNQuantum.jl` function already does (Part 6's black-box note). `CairoMakie` is added to `NNQuantum.jl`'s own `using` list — it was already a direct `Project.toml` dependency (added for `test.jl`'s own plotting, Part 3), so this needed no new `Pkg.add`.

**A second, smaller gap surfaced while making the first one actually useful: nothing produced a *plottable* predicted trajectory.** `FLstep_dynamics_3p` itself — the function `predict_drive_parameters` calls to score a prediction — only ever returns 3 points (each stage's `evolve` call is passed a bare 2-point `tspan`, by design, per (g)'s own status note: correct and cheap for dataset generation, useless for a curve). The only thing that ever produced a dense, plottable trajectory was `test.jl`'s own `run_short_dynamics` — test-local, not reusable outside that one file. Since plotting a *predicted* ladder-step trajectory (not just `test.jl`'s own fixed smoke-test one) is exactly what (v)'s staged validation needs, `run_short_dynamics` was promoted, unchanged, into `FockLadder_problem.jl` as `FLstep_dynamics_3p_dense(t0, initial_state, τ_exc, ωd, τ_SWAP; npoints=200)` — same `H0`/`J`/`π_pulse_shape`, same two-stage hand-off as `FLstep_dynamics_3p`, just `range(...; length=npoints)` per stage instead of a bare 2-tuple. Kept as its own function rather than an `npoints` keyword on `FLstep_dynamics_3p` itself, since the two serve different callers with different needs — (h)/`run_Fock_ladder` want the fast endpoints-only path on every dataset sample; a validation/plotting call wants the slower, dense one, occasionally. `test.jl` was updated to call this shared function instead of its own copy, and its section 3 plotting block was replaced with a call to `plot_trajectory` — both a deduplication and a live verification of the new function, not a separate, unexercised addition.

**Verified**: `test.jl` re-run end-to-end (`julia --project=NNQuantum NNQuantum/test.jl`) — 399-state dense trajectory, `test_plot.png` produced via `plot_trajectory` (unchanged output shape from the old hardcoded block, just produced through the new reusable function), and section 4's dataset smoke test unaffected. `run_Fock_ladder(1, 99; n_samples=6, dim_parameters_space=[3,3,3], epochs=3, save_dir=<tmp>)` re-run in full afterward — no regression from `NNQuantum.jl` gaining a new `using CairoMakie`. Separately, `FLstep_dynamics_3p_dense` was called directly on a real predicted `(τ_exc,ωd,τ_SWAP)` triple (from that same `run_Fock_ladder` run) and its output plotted with `plot_trajectory` — the actual (v)-shaped use case, not just the fixed-parameter smoke test — producing a well-formed 399-point trajectory and a correctly keyed `values` dict (`"n_qubit"`, `"n_osc"`).

`NNQuantum_API.md` gained a new "Plotting a trajectory" section documenting `plot_trajectory` (with a worked example using `FLstep_dynamics_3p_dense`), an overview-table row, and a `using CairoMakie` line in the dependencies block — per the user's new standing rule that any function landing in `NNQuantum.jl` gets `NNQuantum_API.md` updated in the same pass, not deferred.

---

## Part 12 — (v)'s staged validation, run and confirmed: `run_Fock_ladder` completes and chains correctly for N_steps=1 and N_steps=2

With Part 11's two prerequisites in place, (v) itself (Part 6's staged validation) was run this session — with one scoping decision, made explicitly by the user before starting: **not** by editing `FockLadder_execution.jl`'s own `run_Fock_ladder`, but by copying it (plus `generate_decom_basis` and its small path/per-step helpers) into `test.jl`, so the "production" function stays exactly as it was while the validation-specific additions (saving predictions, plotting trajectories) get exercised somewhere disposable-feeling first. `FockLadder_execution.jl` was not touched anywhere in this Part.

**Three things added to the `test.jl` copy, not present in the original:**

1. **Predicted parameters are now saved**, per step, via two new generic functions added to `NNQuantum.jl` — `save_prediction`/`load_prediction` (a predicted-output vector plus the infidelity it scored, same "reconstructible spec" shape as `save_operator_basis`/`save_nn`) — to `predicted_step<n>_b<id>.jld2`. This closed a real gap: previously a prediction was only ever `println`'d, never persisted.
2. **Each step's predicted trajectory is plotted**, via `FLstep_dynamics_3p_dense` (Part 11) re-run on that step's own predicted `(τ_exc,ωd,τ_SWAP)` and its pre-advance `(t0, initial_state)`, then `plot_trajectory` (Part 11) with `["n_qubit"=>..., "n_osc"=>...]`, saved to `trajectory_step<n>_b<id>.png`.
3. **One additional cumulative plot**, `trajectory_full_b<id>.png` — raised as a question by the user before running the N_steps=2 case: does the existing per-step plot show only that step's own window, or the whole run? It was only ever the former (each step's dense trajectory starts at that step's own non-zero `t0`, not `0`, and each gets its own file) — for a multi-step run, that means no single plot shows whether `⟨n_osc⟩` actually climbs `0→1→2→...` across the full run, only isolated snapshots of each individual swap. Fixed by accumulating every step's dense `(tspan, states)` into running `all_tspan`/`all_states` vectors (dropping each step-after-the-first's leading point, which duplicates the previous step's own last point — same convention `FLstep_dynamics_3p`/`_dense` already use to join their own two stages) and calling `plot_trajectory` once more after the loop, with `vlines` at every stage/step boundary. Kept alongside the per-step plots, not instead of them, per the user's own choice among the options presented.

**Return value changed to match `old_version`'s own equivalent function, per explicit request.** The original `run_Fock_ladder` (both the untouched `FockLadder_execution.jl` version and, before this change, the `test.jl` copy) returns `nothing`. The user asked for a return value instead, matching whatever `Chu_DFL_execution.ipynb`'s own equivalent returns — checked directly rather than assumed: the `:FL_1step_3p`/`:master_dynamic` cell (`dfd162e7`, the one this project already mirrors throughout) ends with `return SUN_basis, infidelities, predictions, models, final_states`. The `test.jl` copy now returns exactly that shape — `(decom_basis, infidelities, predictions, models, final_states)` — accumulated the same way `old_version`'s own loop does (`push!` each step). One deliberate difference: `models` holds this project's own richer per-step bundle (`train_NN`'s/`load_nn`'s `NamedTuple` — model plus normalization stats) rather than `old_version`'s bare `Flux.Chain`, since that bundle is what a caller (here, the driver code writing `test_log.md`) actually needs (e.g. `nn.test_error`), not just the model.

**The runs themselves, and what they showed.** Both used the same small/fast hyperparameters, chosen in discussion before running: `n_samples=150`, `epochs=60`, `hidden=128`, `η=1e-3`, `batch_size=32`, `train_fraction=0.85`, uniform continuous sampling, `basis_id=1`.

- **N_steps=1**: predicted `(τ_exc,ωd,τ_SWAP) ≈ (0.00443, -15790.8, 0.005965)`, infidelity **0.0439**, NN test error 0.0469. The user reviewed `trajectory_step1_b1.png` directly (sent via `SendUserFile`) — showed the expected shape: `⟨n_qubit⟩` rising to ~1 during the spin-flip stage, then swapping down to `⟨n_osc⟩` rising to ~0.9 during the SWAP stage, crossing right at the stage boundary.
- **N_steps=2**: step 1 was *reused*, not regenerated — `nn_mode=[:fixed, :train_new]` loads step 1's already-saved, already-reviewed NN and re-runs only the (deterministic) prediction step on it, reproducing the exact same output as the N_steps=1 run bit-for-bit (confirmed: infidelity `0.04385537646450843` both times). Step 2 trained fresh on top of that: predicted `(τ_exc,ωd,τ_SWAP) ≈ (0.00500, -15823.0, 0.004257)`, infidelity **0.0807**, NN test error 0.0429. The cumulative plot (`trajectory_full_b1.png`, also sent) showed the intended picture: `⟨n_osc⟩` stepping `0 → ~0.9 → ~1.8` across two clean rungs, each driven by the qubit population rising then falling back to ~0 — the ladder actually climbing, chained from what a real, imperfect prediction reached rather than from the target state (the entire point of the Part 5 restructure this validation is checking).

To avoid silently clobbering the already-reviewed step-1 result on a second script run, the driver code in `test.jl` guards the step-1-only call: it only runs if `dataset_step1_b1.jld2`/`nn_step1_b1.jld2`/`predicted_step1_b1.jld2`/`trajectory_step1_b1.png` don't already exist for `basis_id=1`. `test_log.md` (new file) records both runs as separate, append-only `##` sections — never rewritten, per the user's own instruction — each ending with a pipeline-correctness check (mechanical: well-formed states, trace preserved, files present) and a separate "visual validity" line, initially "pending user review," updated to **user-confirmed success** for both runs once the user looked at the plots and said so, plus a short "Overall verdict" section: (v)'s staged validation is complete — the 0-4 train/predict/reiterate loop, including the Part 5 restructure's core point (chaining from a *predicted*, not target, state), works correctly at this small/fast scale.

**Left open, not part of this Part**: whether/when to promote the `test.jl`-local `run_Fock_ladder`/`generate_decom_basis` copy (now strictly more capable — saves predictions, plots per-step and cumulative trajectories, returns a summary) back into `FockLadder_execution.jl`'s own version, replacing it rather than living alongside it as a parallel copy. Not decided here — the user's own scoping choice for this session was explicitly to keep them separate.

**Cleanup, once the results were captured in `test_log.md`**: the run's own generated artifacts — `decom_basis_b1.jld2`, `dataset_step1_b1.jld2`/`dataset_step2_b1.jld2`, `nn_step1_b1.jld2`/`nn_step2_b1.jld2`, `predicted_step1_b1.jld2`/`predicted_step2_b1.jld2`, `trajectory_step1_b1.png`/`trajectory_step2_b1.png`/`trajectory_full_b1.png` — were deleted from the local `NNQuantum/` folder at the user's request. Safe to do without any git-side consequence: all ten were untracked (`git status` showed `??`, never `git add`ed or committed in Part 12's own commit), so plain deletion touched nothing in git history and left every tracked file untouched — confirmed by `git status` showing a clean tree immediately after. `test_log.md`'s own written record (predicted parameters, infidelities, test errors, user-confirmed visual validity) remains the durable evidence of this validation; the underlying `.jld2`/`.png` files were only ever meant to be regenerable outputs of a `test.jl` run with `basis_id=1`, per the project's existing "generated artifact, not committed" convention (Part 4).

---

## Part 13 — Promotion: `test.jl`'s validated `run_Fock_ladder` becomes the one in `FockLadder_execution.jl`; the three-step plan is closed

Part 12 left one question open: whether to promote the `test.jl`-local, now-validated `run_Fock_ladder`/`generate_decom_basis` copy back into `FockLadder_execution.jl`'s own version, which until this point was still the earlier, never-validated one (Part 5's restructure — correct code, but never run through (v)'s staged validation itself, since the user's own scoping choice for Part 12 was to validate the copy, not the original). The user asked for this promotion explicitly, together with closing out the three-step plan (`CLAUDE.md`'s Plan section) now that both remaining open items — real-scale runs, and this promotion — had a clear disposition: real-scale runs deferred to the user; promotion, done now.

**What changed, concretely:**

- `FockLadder_execution.jl`'s `generate_decom_basis` was already byte-for-byte identical to `test.jl`'s copy (Part 12 never had to change it) — untouched.
- `FockLadder_execution.jl`'s `run_Fock_ladder` gained everything `test.jl`'s copy added for validation purposes, now as its standard behavior rather than a validation-only extra: three new path helpers (`_predicted_path`, `_trajectory_plot_path`, `_full_trajectory_plot_path`), the four accumulators (`infidelities`, `predictions`, `models`, `final_states`, plus `all_tspan`/`all_states`/`boundary_times` for the cumulative plot), a `save_prediction` call and a per-step `FLstep_dynamics_3p_dense`+`plot_trajectory` call inside the loop, a cumulative `plot_trajectory` call after the loop, and the `return decom_basis, infidelities, predictions, models, final_states` in place of the old `nothing`. The docstring was extended to document the new saved/plotted artifacts and the return value, and to record that this version was staged-validated before being promoted here (pointing to this Part and `test_log.md`). The only textual difference from `test.jl`'s copy: description strings that said "(validation run, ...)" were reworded to drop that qualifier, since this is now the production path, not a validation-only one.
- `test.jl` reverted to its original scope — sections 1-4 (the dynamics/plotting smoke test, Part 3; the dataset-generation smoke test, Part 4) — with section 5 (the validation copy plus its two-driver-block script, Parts 11-12) removed and replaced by a short comment pointing to `FockLadder_execution.jl` and this Part. The now-unneeded `using Dates` (only used for `test_log.md` timestamps in the removed driver code) was also removed.

**Nothing about the algorithm changed** — this is a promotion (moving already-validated code to its permanent home) and a cleanup (removing the now-redundant copy), not a redesign. The five-step (0-4) generate/train/predict/reiterate loop, the `basis_id`-based file naming, and the `dataset_mode`/`nn_mode` reuse controls are exactly as Parts 5-9 left them.

**Verified after promotion:**
1. `julia --project=NNQuantum -e 'include("NNQuantum/FockLadder_execution.jl")'` — loads cleanly, `run_Fock_ladder` resolves to exactly one method (the new one).
2. A fresh end-to-end call, `generate_decom_basis(99; save_dir=tmp)` then `run_Fock_ladder(2, 99; n_samples=6, dim_parameters_space=[3,3,3], epochs=3, hidden=8, save_dir=tmp)` in a scratch temp directory — both steps completed, producing `decom_basis_b99.jld2`, `dataset_step{1,2}_b99.jld2`, `nn_step{1,2}_b99.jld2`, `predicted_step{1,2}_b99.jld2`, `trajectory_step{1,2}_b99.png`, and `trajectory_full_b99.png` (10 files total), with well-formed infidelities (`[0.174, 0.460]` at these tiny/fast hyperparameters — not a claim of accuracy, just that the promoted code runs exactly like the pre-promotion copy did) and the `(decom_basis, infidelities, predictions, models, final_states)` return unpacking correctly.
3. `julia --project=NNQuantum NNQuantum/test.jl` — re-run end-to-end after the section-5 removal, unaffected: still produces `test_plot.png` (399-state trajectory) and `dataset_step1.jld2` (5-sample dataset) exactly as before.
4. Both runs' own generated artifacts were cleaned up from `NNQuantum/` afterward (scratch/tempdir output for the `run_Fock_ladder` call; `test_plot.png`/`dataset_step1.jld2` from the `test.jl` re-run — the latter two turned out to already be **tracked** in git, unlike the untracked-artifact convention documented elsewhere in this file (Part 4, Part 12), so they were restored via `git checkout --` rather than left deleted; worth a note for future sessions, since it means not every `.jld2`/`.png` this project produces is actually untracked, despite the stated convention).

**Plan closure.** With this promotion done and real-scale runs explicitly deferred to the user, `CLAUDE.md`'s three-step Plan is marked closed — see that file's own "Plan — CLOSED" summary, which now points here for the full history rather than repeating it. This `DESIGN.md` file keeps everything above (Parts 1-13) as the permanent working-history record; a new goal, when one arrives, gets its own new Part here and its own new numbered step in `CLAUDE.md`'s Plan, rather than reopening this one.

---

## Part 14 — New goal: resonant single-stage B-spline drive variant (discussion, nothing implemented)

The new goal referenced by `CLAUDE.md`'s new (reopened) Plan section. Introduced by the user via a short document, "Fock State Preparation / NN," Section I ("SYSTEM") plus subsection A ("New task"), describing a second, independent control protocol on the same qubit+HBAR system `:FL_1step_3p` already uses — not a revision of it. This Part records the document's own content and the discussion that resolved its open points; **no code exists for this yet**, and the copy-pasteable LaTeX rewrite the user asked for is reproduced at the end of this Part.

### The document's own framing

Same lab-frame system as `:FL_1step_3p`'s starting point: qubit `Hq = (ℏωq/2)σz`, HBAR mode `Hm = ℏωm b†b`, dipole coupling `Hint = g(b+b†)σx`, and a general drive `Hdrive = ℏ(Ω(t)σ+e^{iωdt} + Ω*(t)σ−e^{-iωdt})`, giving `Hlab = Hq+Hm+Hint+Hdrive`.

The new task (its own subsection A) proposes a *modified* Hamiltonian,
```
Hon = g(σ+b + σ−b†) + Ω(t)σ+ + Ω*(t)σ− = g(σ+b + σ−b†) + ΩRe(t)σx − ΩIm(t)σy,
```
and asks `NNQuantum` to predict the drive functions `ΩRe(t)`/`ΩIm(t)` (decomposed on a B-spline basis) that reproduce the Fock ladder at each step, "in the same fashion of the three-parameters drive already implemented" — i.e. a second variant alongside `:FL_1step_3p`, not its replacement. Two structural differences from `:FL_1step_3p` are stated directly in the document: the pulse duration/time support is no longer an NN output (previously it was) — instead it's sampled from a space, used to generate the dynamics, and fed into the NN as an input feature; and the NN's output layer is the B-spline coefficient decomposition rather than three scalar pulse parameters.

### Discussion, resolving the document's open points

**1. What frame, and what happens to the bare energy term?** `Hon` as written carries no `ωq`/`ωm` term at all, unlike `:FL_1step_3p`'s rotating-frame `H0` (which keeps a `Δ0·n_q` term, `Δ0=ωq-ωm`). User's answer: this variant assumes full resonance, `ωm=ωq=ωd`, so `Δ0=0` exactly — a genuinely different, more special case than `:FL_1step_3p`'s generic `Δ0≠0`, not a relabeling of the same frame. Concretely, this variant's `Qubit`/`HarmonicOscillator` are both built at `ω=0`, so the composite system's bare Hamiltonian vanishes identically, leaving only the static JC coupling and the time-dependent drive — needs its **own** `CompositeSystem`, separate from `:FL_1step_3p`'s (different constructor frequencies), coexisting in `FockLadder_problem.jl` rather than replacing it.

**2. One stage or two?** User's answer: single stage, one continuous drive window governed by `ΩRe(t)`/`ΩIm(t)` together — no spin-flip/SWAP split. This replaces `:FL_1step_3p`'s two sequential `evolve` calls with one `evolve` call over `[t0,t0+T]`; the free-form complex drive is expected to do both jobs (flip and swap) within that one window, which is plausible only *because* the drive shape is now free-form (spline-decomposed) rather than a fixed pulse shape — a fixed-shape single drive couldn't do this, which is presumably why `:FL_1step_3p` needed two stages in the first place.

**Verified algebraically** (not stated in the document, checked in discussion): with `σ± = (σx±iσy)/2` and `Ω=ΩRe+iΩIm`,
```
Ωσ+ + Ω*σ− = ΩReσx − ΩImσy
```
exactly, confirming `Hon` is correct as written and needs no additional carrier term (`e^{iωdt}`) — the resonant rotating frame already absorbs it, unlike `Hdrive`'s lab-frame form.

**3. B-spline hyperparameters.** Deliberately deferred — see "Left open" in `CLAUDE.md`'s Plan section (package choice, order, coefficient count, knot placement, whether `ΩRe`/`ΩIm` share a knot vector). Flagged by the user explicitly as "to be checked and updated later," not part of this discussion pass.

**4. Infidelity — one, at the end, for now.** User confirmed: a single infidelity (final state vs. target), computed once at the end of the trajectory — not `:FL_1step_3p`'s two (spin-flip-stage infidelity + full-protocol infidelity), consistent with there being only one stage now. User raised, but explicitly deferred, a possible future extension: computing infidelity at intermediate checkpoints along the trajectory (e.g. quarter-fractions of `T`) for potentially richer supervision — noted here for later, not designed.

A side effect worth recording: `:FL_1step_3p`'s NN input width is `d²` (decom_basis) + 2 (two infidelities). This variant's is `d²` + 1 (one infidelity) + 1 (duration `T`) — the same total width, `d²+2`, with one infidelity slot repurposed as a duration slot. Not something the user asked for; noticed while comparing the two feature-vector layouts, and flagged to the user as a nice consistency check rather than assumed to be load-bearing.

**5. Coexistence, confirmed.** User: "`:FL_1step_3p` still exists, this is a variant. So keep both in `FockLadder_problem.jl`." Settles the scope question directly — this is additive, not a migration.

### Left open (unchanged from `CLAUDE.md`'s Plan section, not repeated in full here)

B-spline package/order/coefficient-count/knot-placement; whether `decom_basis` is shared between the two variants or this one gets its own; whether the four dissipators (`thermal_bath`, `Dephasing`, `Decay`) really do carry over unchanged onto the resonant composite system (assumed, not yet confirmed by the user); naming for this variant's functions.

### Copy-pasteable LaTeX (the document, rewritten complete with the above)

```latex
\documentclass[11pt]{article}
\usepackage{amsmath,amssymb}

\begin{document}

\section{System}

The system under analysis consists of a superconducting qubit coupled to a
HBAR. The former is described as a two-level quantum system with transition
frequency $\omega_q$. The two energetic levels, in the computational basis,
are written as $|0\rangle_q = \begin{bmatrix}1\\0\end{bmatrix}$ and
$|1\rangle_q = \begin{bmatrix}0\\1\end{bmatrix}$, i.e., eigenvectors of the
$z$-Pauli operator $\sigma_z$. The qubit Hamiltonian is given by
$\mathcal{H}_q = \dfrac{\hbar\omega_q}{2}\sigma_z$. The qubit is tuned by a
drive whose general time-dependent Hamiltonian is
\[
  \mathcal{H}_{\mathrm{drive}} = \hbar\left(\Omega(t)\sigma_+ e^{i\omega_d t}
  + \Omega^*(t)\sigma_- e^{-i\omega_d t}\right),
\]
with complex time-dependent amplitude $\Omega(t)$ and frequency $\omega_d$.

The HBAR is modelled as a high-$Q$ single-mode mechanical resonator with
characteristic frequency $\omega_m$ and Hamiltonian
$\mathcal{H}_m = \hbar\omega_m n = \hbar\omega_m b^\dagger b$, where
$n = b^\dagger b$ represents the number of mode excitations or phonons, and
$b$, $b^\dagger$ are respectively the annihilation/creation operators (zero
point energy is neglected).

The qubit-resonator interaction is a dipole-coupling-like term described by
the bilinear Hamiltonian $\mathcal{H}_{\mathrm{int}} = g(b+b^\dagger)\sigma_x$,
modulated by the interaction strength $g$. Collecting these ingredients, the
Hamiltonian in the laboratory frame is
\begin{equation}
  \mathcal{H}_{\mathrm{lab}} = \mathcal{H}_q + \mathcal{H}_m
  + \mathcal{H}_{\mathrm{int}} + \mathcal{H}_{\mathrm{drive}}.
  \label{eq:Hlab}
\end{equation}

\subsection{New task}

The new task to run on \texttt{NNQuantum} is to predict the best functions
$\Omega_{\mathrm{Re}}(t)$ and $\Omega_{\mathrm{Im}}(t)$, representing the
drive in a modified HBAR-qubit Hamiltonian,
\begin{equation}
  \mathcal{H}_{\mathrm{on}}
    = g\left(\sigma_+ b + \sigma_- b^\dagger\right)
      + \Omega(t)\sigma_+ + \Omega^*(t)\sigma_-
    = g\left(\sigma_+ b + \sigma_- b^\dagger\right)
      + \Omega_{\mathrm{Re}}(t)\sigma_x - \Omega_{\mathrm{Im}}(t)\sigma_y,
  \label{eq:Hon}
\end{equation}
which are able to reproduce the Fock ladder at each step, in the same
fashion as the three-parameter drive already implemented
(\texttt{:FL\_1step\_3p}). This is a genuinely new protocol variant,
coexisting with \texttt{:FL\_1step\_3p} rather than replacing it.

\subsubsection{Resonance condition}

This variant assumes the fully resonant regime,
\begin{equation}
  \omega_m = \omega_q = \omega_d,
  \label{eq:resonance}
\end{equation}
so that the qubit-oscillator detuning $\Delta_0 = \omega_q-\omega_m$
vanishes exactly. Unlike \texttt{:FL\_1step\_3p}'s rotating frame (which
keeps a residual $\Delta_0\, n_q$ term), the bare energy term vanishes
identically here — both subsystems are constructed at $\omega=0$, and
Eq.~\eqref{eq:Hon} is the *complete* Hamiltonian, with no additional
detuning/bare term to add. The complex drive envelope is
\begin{equation}
  \Omega(t) = \Omega_{\mathrm{Re}}(t) + i\,\Omega_{\mathrm{Im}}(t),
  \label{eq:Omega-complex}
\end{equation}
and, using $\sigma_\pm = (\sigma_x \pm i\sigma_y)/2$, one directly checks
$\Omega\sigma_+ + \Omega^*\sigma_- = \Omega_{\mathrm{Re}}\sigma_x -
\Omega_{\mathrm{Im}}\sigma_y$, confirming Eq.~\eqref{eq:Hon} needs no
separate carrier term — the resonant rotating frame already absorbs it.

\subsubsection{Single-stage protocol}

Unlike \texttt{:FL\_1step\_3p}'s two-stage protocol (a spin-flip stage
followed by a SWAP stage), this variant uses a single continuous drive
window: the dynamics are generated by Eq.~\eqref{eq:Hon} over one interval
$[t_0, t_0+T]$, with no intermediate hand-off. The pulse duration/time
support $T$ is not fixed in advance (unlike $\tau_{\mathrm{exc}}$/
$\tau_{\mathrm{SWAP}}$, previously outputs of the NN); instead $T$ is
sampled from a to-be-chosen space and used both to generate that sample's
dynamics and as a feature in the NN's input layer.

\subsubsection{Drive parameterization}

$\Omega_{\mathrm{Re}}(t)$ and $\Omega_{\mathrm{Im}}(t)$ are continuous
functions, each decomposed on a B-spline basis $\{B_k\}_{k=1}^{n_b}$ over
$[0,T]$:
\begin{equation}
  \Omega_{\mathrm{Re}}(t) = \sum_{k=1}^{n_b} c_k^{\mathrm{Re}} B_k(t), \qquad
  \Omega_{\mathrm{Im}}(t) = \sum_{k=1}^{n_b} c_k^{\mathrm{Im}} B_k(t),
  \qquad t\in[0,T].
  \label{eq:bspline}
\end{equation}
Spline order, coefficient count $n_b$, and knot placement are left open —
to be checked and updated later.

\subsubsection{Dataset and network structure}

For each sampled duration $T$ and each sampled coefficient set
$(c^{\mathrm{Re}}, c^{\mathrm{Im}})$, the dynamics generated by
Eq.~\eqref{eq:Hon}/\eqref{eq:bspline} over $[t_0,t_0+T]$ produce a final
state $\rho(t_0+T)$. One dataset row is built as:
\begin{itemize}
  \item \textbf{NN input}: expectation values of $\rho(t_0+T)$ on a random
    orthonormal Hermitian operator basis (the same construction
    \texttt{:FL\_1step\_3p} already uses) $\oplus$ the infidelity between
    $\rho(t_0+T)$ and the target state $\oplus$ the duration $T$.
  \item \textbf{NN output}: the B-spline coefficients
    $(c^{\mathrm{Re}}, c^{\mathrm{Im}})$.
\end{itemize}
The target state for ladder step $n$ is
$|{\downarrow}\rangle_q \otimes |n\rangle_{\mathrm{osc}}$ directly — no
intermediate spin-flip target, since there is no separate stage. The
infidelity is computed once, at the final time, for now; computing it at
intermediate checkpoints (e.g. quarter-fractions of $T$) for richer
supervision is a possible future extension, not part of this design.

\end{document}
```
